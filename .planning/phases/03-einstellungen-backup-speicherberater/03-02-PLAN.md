---
phase: 03-einstellungen-backup-speicherberater
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/services/backupService.ts
  - tests/unit/backupService.test.ts
autonomous: true
requirements:
  - BKP-01
  - BKP-02
  - BKP-03
must_haves:
  truths:
    - "Nutzer kann ein schema-versioniertes Vollbackup lokal exportieren."
    - "Nutzer sieht vor Restore eine Vorschau inkl. Version, Zeitstempel und Datensatzanzahl."
    - "Ungültige Backups stoppen vor Restore ohne Datenverlust."
  artifacts:
    - path: "src/services/backupService.ts"
      provides: "Export/Preview/Restore-Orchestrierung"
    - path: "tests/unit/backupService.test.ts"
      provides: "Fehlerpfad- und Vollrestore-Abdeckung"
  key_links:
    - from: "src/services/backupService.ts"
      to: "src/db/database.ts"
      via: "Dexie transaction for full restore"
      pattern: "transaction|clear|bulkAdd"
---

<objective>
Implementiert einen sicheren, schema-versionierten Backup/Restore-Flow auf Service-Ebene.

Purpose: Backup ist die einzige Migrationsroute zwischen Browsern/Geraeten und muss fehlertolerant sein.
Output: Backup-Service mit Export, Preview, Validierung und transaktionalem Voll-Restore plus Unit-Tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-UI-SPEC.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-RESEARCH.md
@src/db/database.ts
@src/db/schema.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: RED/GREEN für Backup-JSON-Vertrag und Vorschau</name>
  <files>src/services/backupService.ts, tests/unit/backupService.test.ts</files>
  <behavior>
    - Test 1: Export enthält `schemaVersion`, `exportedAt` und alle relevanten Collections.
    - Test 2: Vorschau liefert Record-Counts je Domäne.
    - Test 3: Schemafehler oder ungültiges JSON erzeugen blockierenden Fehler.
  </behavior>
  <action>Schreibe fehlschlagende Tests für Backup-Vertrag und Preview. Implementiere danach `exportBackup` und `previewBackup` mit versioniertem Payload und klaren Fehlercodes für invalide Dateien.</action>
  <verify><automated>node --test tests/unit/backupService.test.ts</automated></verify>
  <done>Export und Preview sind vollständig spezifiziert und grün getestet.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Voll-Restore transaktional und fail-closed absichern</name>
  <files>src/services/backupService.ts, tests/unit/backupService.test.ts</files>
  <behavior>
    - Test 1: Gültiges Backup ersetzt Daten vollständig innerhalb einer Transaktion.
    - Test 2: Ungültiges Backup verändert keine bestehende Tabelle.
  </behavior>
  <action>Implementiere `restoreBackup` als expliziten Voll-Restore (`clear` + `bulkAdd`) über alle Phase-3-relevanten Tabellen in einer Dexie-Transaktion. Bei Validierungs-/Parsingfehlern muss der Restore vor jeder Mutation abbrechen.</action>
  <verify><automated>node --test tests/unit/backupService.test.ts</automated></verify>
  <done>BKP-01 bis BKP-03 sind service-seitig belastbar umgesetzt.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Externe Backup-Datei → Restore-Service | Untrusted JSON mit möglicher Schema-/Datenmanipulation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T | backup restore input | mitigate | Schema-Version, Struktur und Pflichtfelder vor Mutation strikt validieren |
| T-03-04 | D | restore transaction | mitigate | Restore nur innerhalb einer Dexie-Transaktion; Fehler => Rollback |
</threat_model>

<verification>
- node --test tests/unit/backupService.test.ts
</verification>

<success_criteria>
- Export, Vorschau und Voll-Restore verhalten sich deterministisch und fehlertolerant.
- Kein invalider Import kann bestehende Daten überschreiben.
</success_criteria>

<output>
After completion, create `.planning/phases/03-einstellungen-backup-speicherberater/03-02-SUMMARY.md`
</output>
