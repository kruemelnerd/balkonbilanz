---
phase: 03-einstellungen-backup-speicherberater
plan: 08
type: execute
wave: 7
depends_on: [03-07]
files_modified:
  - src/features/settings/SettingsView.vue
  - src/features/settings/BatteryAdvisorCard.vue
  - src/services/batteryAdvisorService.ts
  - tests/unit/batteryAdvisorService.test.ts
  - tests/component/batteryAdvisorCard.test.ts
  - tests/e2e/mobile-settings-battery.spec.ts
autonomous: true
gap_closure: true
requirements: [BATT-03, SET-02]
must_haves:
  truths:
    - "Nutzer sieht im Speicherberater vier Szenarien, deren Einsparung auf realen Analyseergebnissen (nicht nur Zeitraumlaenge) basiert."
    - "Nach Speichern geaenderter Einstellungen nutzt die naechste Berechnung ohne Remount den persistierten Strompreis."
    - "Break-even und Warnhinweise bleiben konsistent mit Analysequalitaet und Analysebasis."
  artifacts:
    - path: src/services/batteryAdvisorService.ts
      provides: analysis-output-based annual savings and break-even calculation contract
    - path: src/features/settings/SettingsView.vue
      provides: refreshed advisor context sourced from analysisStore outputs and persisted settings
    - path: src/features/settings/BatteryAdvisorCard.vue
      provides: reactive recalculation when advisor snapshot props change
    - path: tests/e2e/mobile-settings-battery.spec.ts
      provides: mobile proof that saved price and analysis-driven basis update scenario outputs
  key_links:
    - from: src/stores/analysisStore.ts
      to: src/features/settings/SettingsView.vue
      via: combined/interval/pv outputs converted to advisor snapshot basis
      pattern: "analysisStore\\.(combined|intervals|pvDays|quality)"
    - from: src/features/settings/SettingsView.vue
      to: src/features/settings/BatteryAdvisorCard.vue
      via: snapshot updates after saveSettings and loadAnalysis
      pattern: "batteryAdvisorSnapshot"
    - from: src/features/settings/BatteryAdvisorCard.vue
      to: src/services/batteryAdvisorService.ts
      via: calculate() with updated snapshot-derived draft
      pattern: "service.calculate"
---

<objective>
Schliesst den verbleibenden Phase-3-Gap: Speicherberater liefert echte nutzerdatengetriebene Einsparungs- und Break-even-Aussagen.

Purpose: Die letzte offene Wahrheit aus 03-VERIFICATION.md wird vollstaendig geschlossen, damit BATT-03 nicht mehr als hollow/generic formula eingestuft wird.
Output: Reaktive Advisor-Verdrahtung mit Analyse-basiertem Rechenkern plus zielgerichtete Regressionstests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-VERIFICATION.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-DISCUSS-CHECKPOINT.json
@src/stores/analysisStore.ts
@src/features/settings/SettingsView.vue
@src/features/settings/BatteryAdvisorCard.vue
@src/services/batteryAdvisorService.ts

<interfaces>
From src/stores/analysisStore.ts:
```ts
export interface AnalysisStore {
  intervals: MeterIntervalResult[];
  pvDays: PvDailyRecord[];
  combined: CombinedKpiResult | null;
  quality: DataQualityResult | null;
  fromDay: string;
  toDay: string;
  loadAnalysis(): Promise<void>;
}
```

From src/services/batteryAdvisorService.ts:
```ts
export interface BatteryAdvisorInput {
  storagePriceEur: number;
  capacityKwh: number;
  efficiency: number;
  analysisPeriodDays: number;
  qualityLevel: DataQualityLevel;
  electricityPriceEurPerKwh?: number;
}

export interface BatteryAdvisorService {
  calculate(input: BatteryAdvisorInput): Promise<BatteryAdvisorResult>;
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: RED — echte Analysebasis und Context-Refresh als failing Regression abbilden</name>
  <files>tests/unit/batteryAdvisorService.test.ts, tests/component/batteryAdvisorCard.test.ts, tests/e2e/mobile-settings-battery.spec.ts</files>
  <behavior>
    - Test 1: Service nutzt eine explizite analysebasierte Energie-Basis (z. B. aus combined/export/self-consumption Kontext) statt nur analysisPeriodDays.
    - Test 2: Nach saveSettings() und erneutem Berechnen wird der neu persistierte Strompreis ohne Remount wirksam.
    - Test 3: Poor-Warnung bleibt analysequalitaetsgetrieben und koexistiert mit der neuen Einsparungsbasis.
  </behavior>
  <action>Erweitere die vorhandenen Tests gezielt fuer den verbleibenden Gap aus 03-VERIFICATION.md: Der aktuelle Rechenkern darf nicht mehr allein von `annualCyclesFromPeriodDays` abhaengen. Verankere die Discuss-Entscheidung "Aktueller Analysezeitraum" explizit in den Erwartungen (per Entscheidung aus 03-DISCUSS-CHECKPOINT.json), und schreibe mobile/component Assertions, die Context-Refresh nach Settings-Save beweisen.</action>
  <verify>
    <automated>node --test tests/unit/batteryAdvisorService.test.ts tests/component/batteryAdvisorCard.test.ts tests/e2e/mobile-settings-battery.spec.ts</automated>
  </verify>
  <done>Tests schlagen initial fehl und belegen reproduzierbar die fehlende nutzerdatengetriebene Berechnungsbasis bzw. den fehlenden Refresh.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — Advisor-Rechenkern auf Analyse-Outputs umstellen und Snapshot reaktiv refreshen</name>
  <files>src/features/settings/SettingsView.vue, src/features/settings/BatteryAdvisorCard.vue, src/services/batteryAdvisorService.ts</files>
  <behavior>
    - Berechnungsbasis wird aus realen Analyse-Outputs hergeleitet (nicht nur Tagezahl).
    - SettingsView aktualisiert Advisor-Context nach saveSettings() sowie nach neu geladenen Analysewerten.
    - BatteryAdvisorCard reagiert auf Snapshot-Aenderungen (watch/equivalent) und berechnet mit aktualisiertem Kontext.
  </behavior>
  <action>Setze die in VERIFICATION.md geforderten fehlenden Links um: Leite in `SettingsView.vue` aus `analysisStore.combined`/relevanten Analyse-Outputs eine explizite Basis fuer jaehrliche Einsparung ab, aktualisiere `batteryAdvisorSnapshot` nach erfolgreichem `saveSettings()` sofort mit dem persistierten Preis, und mache die Card gegen spaetere Snapshot-Aenderungen reaktiv statt nur `onMounted()`. Passe `batteryAdvisorService.ts` so an, dass BATT-03 auf realen Analyseergebnissen basiert (mit nachvollziehbarem Fallback nur wenn Daten fehlen) und Warn-/Confidence-Verhalten unveraendert qualitaetsgetrieben bleibt.</action>
  <verify>
    <automated>node --test tests/unit/batteryAdvisorService.test.ts tests/component/batteryAdvisorCard.test.ts</automated>
  </verify>
  <done>Die Berechnung ist nachweisbar analyse-output-basiert; gespeicherte Preisanpassungen wirken ohne Remount auf die naechste Szenarioberechnung.</done>
</task>

<task type="auto">
  <name>task 3: Regression matrix fuer Gap-Closure abschliessen</name>
  <files>tests/unit/batteryAdvisorService.test.ts, tests/component/batteryAdvisorCard.test.ts, tests/e2e/mobile-settings-battery.spec.ts</files>
  <action>Fuehre den kombinierten Unit/Component/Mobile-Regressionslauf aus und bestaetige, dass die offene Wahrheit aus 03-VERIFICATION.md geschlossen ist: Szenariowerte reagieren auf reale Analysebasis + aktualisierte Settings, bei unveraenderter Vier-Szenario-Reihenfolge.</action>
  <verify>
    <automated>node --test tests/unit/batteryAdvisorService.test.ts tests/component/batteryAdvisorCard.test.ts tests/e2e/mobile-settings-battery.spec.ts</automated>
  </verify>
  <done>Alle Advisor-Regressionen sind gruen; BATT-03 ist nicht mehr blockiert/hollow.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis outputs -> financial estimate | Untrusted/stale analysis data can distort savings and break-even messaging |
| persisted settings -> advisor context | User-edited electricity price feeds economic output directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-22 | T | src/services/batteryAdvisorService.ts | mitigate | Validate analysis-derived basis values and reject non-finite/negative inputs before scenario math |
| T-03-23 | I | src/features/settings/SettingsView.vue | mitigate | Rebuild snapshot after successful saveSettings + analysis load so displayed estimates reflect persisted truth |
| T-03-24 | R | src/features/settings/BatteryAdvisorCard.vue | mitigate | Keep explicit warning/confidence text tied to analysis quality and preserve deterministic calculation trigger path |
</threat_model>

<verification>
Gap ist geschlossen, wenn die kombinierte Advisor-Regressionsmatrix gruen ist und der Verifier keine generische Zeitraumformel ohne Analyse-Outputs mehr nachweisen kann.
</verification>

<success_criteria>
- Die verbleibende Gap-Truth aus 03-VERIFICATION.md ist verifiziert.
- BATT-03 ist als SATISFIED nachvollziehbar belegbar.
- Szenariowerte reagieren auf echte Analysebasis plus gespeicherte Preise ohne Seiten-Remount.
</success_criteria>

<output>
After completion, create `.planning/phases/03-einstellungen-backup-speicherberater/03-08-SUMMARY.md`
</output>
