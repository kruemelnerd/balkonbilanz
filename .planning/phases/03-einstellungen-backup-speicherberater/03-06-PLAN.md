---
phase: 03-einstellungen-backup-speicherberater
plan: 06
type: execute
wave: 5
depends_on: [03-05]
files_modified:
  - src/features/settings/SettingsView.vue
  - tests/component/settingsView.test.ts
  - tests/e2e/mobile-settings-battery.spec.ts
autonomous: true
gap_closure: true
requirements: [SET-03, BKP-03]
must_haves:
  truths:
    - "Nutzer kann Tarifperioden in der Einstellungen-UI anlegen, bearbeiten und löschen."
    - "Ungültige Backup-Dateien werden im UI ohne Datenmutation sauber als Fehler abgefangen."
  artifacts:
    - path: src/features/settings/SettingsView.vue
      provides: tariff edit/delete controls plus safe backup file parsing
    - path: tests/component/settingsView.test.ts
      provides: UI regression coverage for tariff management and malformed backup handling
  key_links:
    - from: src/features/settings/SettingsView.vue
      to: src/services/settingsService.ts
      via: saveTariffPeriod({ id? }) and deleteTariffPeriod(id)
      pattern: "saveTariffPeriod|deleteTariffPeriod"
    - from: src/features/settings/SettingsView.vue
      to: src/services/backupService.ts
      via: previewImport/restoreBackup after guarded JSON parsing
      pattern: "try|catch|previewImport|restoreBackup"
---

<objective>
Schließt die verifizierten UI-Lücken für Tarifverwaltung und invalides Backup-Parsing.

Purpose: Ohne diese Korrekturen bleiben SET-03 und BKP-03 trotz vorhandener Services unvollständig.
Output: SettingsView mit Tarif-Bearbeiten/Löschen und robustem Invalid-Backup-Fehlerpfad.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-einstellungen-backup-speicherberater/03-VERIFICATION.md
@src/features/settings/SettingsView.vue
@src/services/settingsService.ts
@src/services/backupService.ts
@tests/component/settingsView.test.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: RED — Regressionstests für Tarif-Edit/Lösch-Flow und malformed-backup UI-Fehler schreiben</name>
  <files>tests/component/settingsView.test.ts, tests/e2e/mobile-settings-battery.spec.ts</files>
  <read_first>tests/component/settingsView.test.ts, tests/e2e/mobile-settings-battery.spec.ts, src/features/settings/SettingsView.vue, .planning/phases/03-einstellungen-backup-speicherberater/03-VERIFICATION.md</read_first>
  <acceptance_criteria>
    - Component-Test enthält eine Assertion auf sichtbare Edit-/Lösch-Aktionen pro Tarifkarte.
    - Component-Test enthält eine Assertion, dass bei malformed JSON ein Inline-Fehler mit Invalid-Backup-Text angezeigt wird.
    - Neuer/angepasster Test schlägt vor Implementierung fehl.
  </acceptance_criteria>
  <behavior>
    - Test 1: Tarifperiode kann aus Karte in den Formularentwurf übernommen und aktualisiert werden.
    - Test 2: Tarifperiode kann gelöscht werden und verschwindet aus der Liste.
    - Test 3: Ungültiger JSON-Upload setzt backupError und lässt backupPreviewReady false.
  </behavior>
  <action>Erweitere die bestehenden node:test-Komponententests um die drei Gap-Verhaltensfälle aus 03-VERIFICATION.md und passe den mobilen Smoke-Test an, sodass er keinen manuellen Qualitäts-Toggle mehr voraussetzt.</action>
  <verify><automated>node --test tests/component/settingsView.test.ts tests/e2e/mobile-settings-battery.spec.ts</automated></verify>
  <done>Failing Tests belegen exakt die beiden offenen Gaps vor Produktionsänderungen.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — SettingsView mit Tarif-Bearbeiten/Löschen und sicherem JSON-Parsing umsetzen</name>
  <files>src/features/settings/SettingsView.vue</files>
  <read_first>src/features/settings/SettingsView.vue, src/services/settingsService.ts, src/services/backupService.ts, src/domain/backup/backupSchema.ts</read_first>
  <acceptance_criteria>
    - Tarifkarten zeigen Buttons mit Texten "Bearbeiten" und "Löschen".
    - Bearbeiten setzt tariffDraft inklusive period.id für Update-Writeback.
    - Löschen ruft deleteTariffPeriod(id) auf und aktualisiert tariffPeriods lokal.
    - readBackupFile und confirmRestore umschließen JSON.parse in try/catch und setzen bei Parse-Fehler backupError ohne Service-Mutation.
  </acceptance_criteria>
  <action>Implementiere pro Tarifkarte explizite Bearbeiten-/Löschen-Aktionen und verdrahte sie mit settingsService.saveTariffPeriod (mit id beim Edit) sowie settingsService.deleteTariffPeriod. Ergänze einen klaren edit mode (z. B. aktive period id + CTA-Text „Tarifperiode aktualisieren“). Ersetze bares JSON.parse in readBackupFile/confirmRestore durch try/catch und setze eine nicht-destruktive Fehlermeldung (z. B. „Ungültige Backup-Datei: kein valides JSON.") gemäß BKP-03.</action>
  <verify><automated>node --test tests/component/settingsView.test.ts</automated></verify>
  <done>Tarifverwaltung ist vollständig (CRUD ohne Overlap) und malformed Backups werden im UI robust abgefangen.</done>
</task>

<task type="auto">
  <name>task 3: Vollständigen Settings/Backup-Regressionslauf ausführen</name>
  <files>tests/component/settingsView.test.ts, tests/e2e/mobile-settings-battery.spec.ts</files>
  <read_first>tests/component/settingsView.test.ts, tests/e2e/mobile-settings-battery.spec.ts</read_first>
  <acceptance_criteria>
    - Node-Testlauf endet mit Exit-Code 0.
    - Kein Test referenziert den manuellen poor/good/limited Toggle als Voraussetzung für Warning-Verifikation.
  </acceptance_criteria>
  <action>Führe den kombinierten Testlauf für Settings/Backup/Battery-Shell-Pfade aus und bestätige, dass die neuen Gaps reproduzierbar geschlossen sind.</action>
  <verify><automated>node --test tests/component/settingsView.test.ts tests/unit/backupService.test.ts tests/e2e/mobile-settings-battery.spec.ts</automated></verify>
  <done>SET-03 und BKP-03 sind automatisiert regressionsgesichert.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file input→backup parser | Untrusted user file content enters restore flow |
| settings UI→settings service | User inputs mutate persisted tariff/settings records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | D | SettingsView backup import | mitigate | Wrap JSON parsing in try/catch and fail closed with non-mutating error state |
| T-03-17 | T | tariff edit/delete actions | mitigate | Require valid period id for delete/update paths and preserve overlap validation in service |
| T-03-18 | R | destructive restore confirmation | accept | Existing preview+confirmed gate remains; this plan does not loosen restore confirmation |
</threat_model>

<verification>
SettingsView component tests und mobile smoke müssen grün sein, inklusive malformed-backup Pfad.
</verification>

<success_criteria>
Verifizierungs-Gaps zu Tarifmanagement und invalid-backup Parsing sind geschlossen und automatisiert abgesichert.
</success_criteria>

<output>
After completion, create `.planning/phases/03-einstellungen-backup-speicherberater/03-06-SUMMARY.md`
</output>
