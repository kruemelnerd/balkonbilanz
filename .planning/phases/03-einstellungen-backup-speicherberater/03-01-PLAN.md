---
phase: 03-einstellungen-backup-speicherberater
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/settings/settingsTypes.ts
  - src/domain/settings/tariffPeriods.ts
  - src/db/schema.ts
  - src/db/database.ts
  - src/repositories/settingsRepository.ts
  - src/services/settingsService.ts
  - tests/unit/tariffPeriods.test.ts
  - tests/unit/settingsService.test.ts
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
must_haves:
  truths:
    - "Nutzer sieht Defaultwerte 0.305 EUR/kWh und 0 EUR/kWh bei Erstnutzung."
    - "Nutzer kann Strompreis, Einspeisevergütung und Qualitätsmodus lokal speichern und wieder laden."
    - "Nutzer kann Tarifperioden nur ohne überlappende Zeiträume speichern."
  artifacts:
    - path: "src/services/settingsService.ts"
      provides: "Load/save-Logik für Settings und Tarifperioden"
    - path: "src/domain/settings/tariffPeriods.ts"
      provides: "Überlappungs- und Feldvalidierung"
    - path: "src/db/schema.ts"
      provides: "Dexie-Schema für appSettings und tariffPeriods"
  key_links:
    - from: "src/services/settingsService.ts"
      to: "src/repositories/settingsRepository.ts"
      via: "CRUD-Orchestrierung"
      pattern: "saveSettings|saveTariffPeriod|deleteTariffPeriod"
---

<objective>
Schafft die persistente Datenbasis und Service-Logik für Einstellungen und Tarifhistorie.

Purpose: Ohne stabile Settings/Tarif-Domäne sind Backup und Speicherberater fachlich nicht belastbar.
Output: Settings-/Tariftypen, Validierung, DB-Tabellen, Repository, Service und Unit-Absicherung.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-UI-SPEC.md
@.planning/phases/03-einstellungen-backup-speicherberater/03-RESEARCH.md
@src/db/schema.ts
@src/db/database.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>task 1: Settings-/Tarif-Domain und RED-Tests definieren</name>
  <files>src/domain/settings/settingsTypes.ts, src/domain/settings/tariffPeriods.ts, tests/unit/tariffPeriods.test.ts, tests/unit/settingsService.test.ts</files>
  <behavior>
    - Test 1: Überlappende Tarifperioden werden mit blockierendem Fehler abgewiesen.
    - Test 2: Fehlende Enddaten sind erlaubt, aber Startdatum/Preis müssen valide sein.
    - Test 3: Default-Settings werden zurückgegeben, wenn noch keine Persistenz existiert.
  </behavior>
  <action>Schreibe zuerst fehlschlagende Unit-Tests für Tarifvalidierung und Settings-Defaults. Implementiere danach Domaintypen (inkl. `relaxed|balanced|strict`) und reine Validierungsfunktionen für nicht überlappende Tarifzeiträume.</action>
  <verify><automated>node --test tests/unit/tariffPeriods.test.ts tests/unit/settingsService.test.ts</automated></verify>
  <done>Domänenregeln sind testgetrieben spezifiziert und reproduzierbar grün.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Dexie-Schema, Repository und Settings-Service verdrahten</name>
  <files>src/db/schema.ts, src/db/database.ts, src/repositories/settingsRepository.ts, src/services/settingsService.ts, tests/unit/settingsService.test.ts</files>
  <behavior>
    - Test 1: `loadSettings()` liefert persistierte Werte oder Defaults.
    - Test 2: `saveTariffPeriod()` verhindert Overlap und speichert valide Perioden.
  </behavior>
  <action>Erweitere DB-Schema um `appSettings` und `tariffPeriods`, implementiere ein Repository mit CRUD-Operationen und einen Service, der Defaults, Persistenz und Tarifvalidierung orchestriert.</action>
  <verify><automated>node --test tests/unit/settingsService.test.ts tests/unit/tariffPeriods.test.ts</automated></verify>
  <done>Settings und Tarifperioden sind lokal persistiert, validiert und service-seitig nutzbar.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Eingabe → Settings-Service | Unvalidierte Preis-, Datums- und Moduswerte passieren die Service-Grenze |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | tariffPeriods validation | mitigate | Überlappungen und ungültige Datumsbereiche in Domain-Validator strikt blockieren |
| T-03-02 | D | settings persistence | mitigate | Fallback auf sichere Defaults bei fehlender/inkonsistenter Persistenz |
</threat_model>

<verification>
- node --test tests/unit/tariffPeriods.test.ts tests/unit/settingsService.test.ts
</verification>

<success_criteria>
- SET-01 bis SET-04 sind in Domain+Service vollständig umgesetzt.
- DB enthält persistente Tabellen für Settings und Tarifperioden.
</success_criteria>

<output>
After completion, create `.planning/phases/03-einstellungen-backup-speicherberater/03-01-SUMMARY.md`
</output>
