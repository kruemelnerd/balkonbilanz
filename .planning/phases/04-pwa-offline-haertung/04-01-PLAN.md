---
phase: 04-pwa-offline-haertung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - package.json
  - src/main.ts
  - src/pwa/registerServiceWorker.ts
  - src/features/pwa/ReloadPrompt.vue
  - src/styles/main.css
autonomous: true
requirements:
  - PWA-01
  - PWA-04
must_haves:
  truths:
    - "Nutzer kann die App nach Erstladen ohne Netz erneut starten."
    - "Nutzer sieht bei neuer Version einen klaren Update-Hinweis mit Reload-Aktion."
  artifacts:
    - path: "vite.config.ts"
      provides: "VitePWA Plugin-Konfiguration mit Manifest und Cachingstrategie"
    - path: "src/pwa/registerServiceWorker.ts"
      provides: "SW-Registrierung + needRefresh/offlineReady State"
    - path: "src/features/pwa/ReloadPrompt.vue"
      provides: "sichtbarer Update/Offline-Toast"
  key_links:
    - from: "src/main.ts"
      to: "src/pwa/registerServiceWorker.ts"
      via: "Bootstrapping"
      pattern: "registerSW|registerServiceWorker"
    - from: "ReloadPrompt.vue"
      to: "updateServiceWorker"
      via: "Reload Button"
      pattern: "needRefresh|updateServiceWorker"
---

<objective>
Aktiviert PWA-Basis (Service Worker, Manifest, Runtime-Caching) und einen klaren Update-Hinweis für neue App-Versionen.

Purpose: Erfüllt den Offline-Start und transparentes Update-Verhalten für Nutzer.
Output: Vite-PWA Konfiguration, SW-Registrierung, ReloadPrompt UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/main.ts
@src/router/index.ts

<interfaces>
From src/router/index.ts:
```ts
export function createAppRouter(options?: CreateAppRouterOptions): Router;
export const appRouter: Router;
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>task 1: Vite PWA Plugin und Manifest konfigurieren</name>
  <files>vite.config.ts, package.json</files>
  <action>Installiere und konfiguriere `vite-plugin-pwa` (per Projekt-Constraint und Stack-Empfehlung) mit `registerType: 'prompt'`, App-Manifest, `navigateFallback: '/'` und konservativer Runtime-Caching-Strategie für statische Assets. Verwende keine externen funktionalen Requests als Pflichtpfad.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Build erzeugt SW/Manifest-Artefakte und die App bleibt als SPA offline navigierbar.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: SW-Registrierung und Update-Prompt UI verdrahten</name>
  <files>src/pwa/registerServiceWorker.ts, src/features/pwa/ReloadPrompt.vue, src/main.ts, src/styles/main.css</files>
  <behavior>
    - Test 1: Bei `needRefresh=true` erscheint ein Update-Hinweis mit Reload-Aktion.
    - Test 2: Bei `offlineReady=true` erscheint ein Offline-Ready-Hinweis.
  </behavior>
  <action>Implementiere eine dedizierte Register-Schicht auf Basis `virtual:pwa-register/vue` und rendere einen mobilen, klar beschrifteten Prompt. UI-Text muss verständlich sein („Neue Version verfügbar“, „Jetzt aktualisieren“). Verdrahte den Prompt global beim App-Start.</action>
  <verify>
    <automated>node --test tests/component/*.test.ts</automated>
  </verify>
  <done>Nutzer bekommen reproduzierbar einen sichtbaren Update-Hinweis und können das Update gezielt anwenden.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser Cache → App Shell | Untrusted/offline cache state beeinflusst ausgelieferten Code |
| SW Lifecycle → UI Prompt | Hintergrundupdate triggert Nutzerentscheidung |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | SW update lifecycle | mitigate | Prompt-Modus mit expliziter Nutzeraktion + klarer Versionshinweis im UI |
| T-04-02 | D | navigate fallback | mitigate | Workbox `navigateFallback: '/'` und Build-Validierung für Offline-Start |
</threat_model>

<verification>
- npm run build
- node --test tests/component/*.test.ts
</verification>

<success_criteria>
- PWA-01: Offline-Start nach Erstladen funktioniert.
- PWA-04: Update-Hinweis bei neuer Version ist sichtbar und auslösbar.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-offline-haertung/04-01-SUMMARY.md`
</output>
