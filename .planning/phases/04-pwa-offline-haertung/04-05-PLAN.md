---
phase: 04-pwa-offline-haertung
plan: 05
type: execute
wave: 2
depends_on: [04-04]
files_modified:
  - tests/bdd/pwa-offline.feature
  - tests/bdd/steps/pwaOffline.steps.ts
  - tests/bdd/pwa-offline.spec.ts
  - tests/unit/pvDailyService.test.ts
  - package.json
autonomous: true
gap_closure: true
requirements: [TEST-01, TEST-04]
must_haves:
  truths:
    - "Mindestens ein ausfuehrbares Gherkin-Szenario deckt den PWA-Offline-Flow ab."
    - "Phase-relevante automatisierte Regressionen laufen gruen."
  artifacts:
    - path: "tests/bdd/pwa-offline.spec.ts"
      provides: "Runner-gebundene Ausfuehrung der Feature-Datei"
    - path: "tests/unit/pvDailyService.test.ts"
      provides: "Zeitstabile Today/Future-Validierung"
    - path: "package.json"
      provides: "BDD-Skript in der Test-Wiring"
  key_links:
    - from: "tests/bdd/pwa-offline.spec.ts"
      to: "tests/bdd/pwa-offline.feature"
      via: "Feature-Runner Integration"
      pattern: "pwa-offline\.feature"
    - from: "tests/unit/pvDailyService.test.ts"
      to: "src/services/pvDailyService.ts"
      via: "dynamisches Heute/Future-Szenario"
      pattern: "today|future|Date"
---

<objective>
Schliesst die offenen Nachweis-Gaps fuer ausfuehrbares Gherkin und stabile gruene Regressionssuite.

Purpose: TEST-04 und TEST-01 sind laut 04-VERIFICATION blockiert, bis Feature-Runner und zeitstabile Unit-Assertion vorhanden sind.
Output: Runner-gebundener BDD-Testpfad fuer PWA-Offline plus stabilisierte Unit-Regression.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-offline-haertung/04-VERIFICATION.md
@.planning/phases/04-pwa-offline-haertung/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED/GREEN — PWA-Offline-Feature an ausfuehrbaren BDD-Runner anbinden</name>
  <files>tests/bdd/pwa-offline.feature, tests/bdd/steps/pwaOffline.steps.ts, tests/bdd/pwa-offline.spec.ts, package.json</files>
  <behavior>
    - Test 1: Das vorhandene `.feature`-Szenario wird vom Runner geladen und ausgefuehrt.
    - Test 2: BDD-Kommando scheitert bei nicht gematchten Steps und wird gruen bei korrekter Verdrahtung.
  </behavior>
  <action>Verdrahte `tests/bdd/pwa-offline.feature` mit einem real ausfuehrbaren Runner-Pfad (projektkonform, z. B. bestehender Node-Test- oder playwright-bdd-Flow). Die Step-Definitionen duerfen nicht als isolierter Standalone-Test verbleiben, sondern muessen vom Feature-Lauf konsumiert werden. Ergaenze in `package.json` ein dediziertes Script fuer diesen PWA-BDD-Lauf.</action>
  <verify><automated>npm run test:bdd:pwa</automated></verify>
  <done>Das PWA-Feature wird ueber Runner ausgefuehrt und liefert einen reproduzierbaren Pass/Fail-Nachweis.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: RED/GREEN — zeitabhaengigen PV-Unit-Test dynamisch auf Heute/Future stabilisieren</name>
  <files>tests/unit/pvDailyService.test.ts</files>
  <behavior>
    - Test 1: "today" wird immer relativ zum Testzeitpunkt berechnet und bleibt blockiert.
    - Test 2: Ein echtes Future-Datum bleibt ebenfalls blockiert.
  </behavior>
  <action>Ersetze den festen Kalendertag im failing Test durch dynamisch berechnete `today`/`future` Werte, sodass die Fachregel dauerhaft stabil verifiziert wird. Keine Lockerung der Fachregel; nur der Testzeitbezug wird korrigiert. Anschliessend die relevante Phase-4-Regression erneut gruen nachweisen.</action>
  <verify><automated>npm run test:unit && npm run test:component && npm run test:smoke</automated></verify>
  <done>Die bisher rote Unit-Suite ist wieder stabil gruen und entwertet den Regressionsnachweis nicht mehr.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature text → Step execution | Freitext aus Gherkin wird in ausfuehrbare Testschritte aufgeloest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | R | tests/bdd/pwa-offline.feature wiring | mitigate | Runner-Skript fixieren und explizit auf die PWA-Feature-Datei richten; fehlende Steps als Fehler behandeln. |
| T-04-04 | D | tests/unit/pvDailyService.test.ts | mitigate | Dynamische Datumserzeugung verwenden, um zeitbedingte Testausfaelle (false negatives) zu verhindern. |
</threat_model>

<verification>
- npm run test:bdd:pwa
- npm run test:unit
- npm run test:component
- npm run test:smoke
</verification>

<success_criteria>
- Das PWA-Gherkin-Szenario ist runner-gebunden und ausfuehrbar.
- Relevante Regressionen sind wieder vollstaendig gruen und zeitstabil.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-offline-haertung/04-05-SUMMARY.md`
</output>
