---
phase: 04-pwa-offline-haertung
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/repositories/meterReadingsRepository.ts
  - src/repositories/pvDailyRepository.ts
  - src/repositories/settingsRepository.ts
  - src/db/schema.ts
  - tests/unit/meterReadingsRepository.test.ts
  - tests/unit/pvDailyRepository.test.ts
  - tests/unit/settingsService.test.ts
  - tests/component/captureOfflinePersistence.test.ts
autonomous: true
requirements:
  - PWA-02
  - PWA-03
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Nutzer kann offline neue Zähler- und PV-Einträge lokal speichern."
    - "Nutzerdaten bleiben nach Reload unverändert vorhanden."
    - "Nutzerdaten überleben ein App-Update ohne Verlust."
  artifacts:
    - path: "tests/unit/meterReadingsRepository.test.ts"
      provides: "Offline/Reload/Update Persistenz-Regressionsfälle für Zählerdaten"
    - path: "tests/unit/pvDailyRepository.test.ts"
      provides: "Offline/Reload/Update Persistenz-Regressionsfälle für PV-Tage"
    - path: "tests/component/captureOfflinePersistence.test.ts"
      provides: "Nutzerfluss-Nachweis für Offline-Speichern im Capture-UI"
  key_links:
    - from: "Capture UI"
      to: "Dexie repositories"
      via: "store save actions"
      pattern: "saveMeter|savePv"
    - from: "Dexie schema"
      to: "existing records"
      via: "versioned upgrade path"
      pattern: "db\.version|upgrade"
---

<objective>
Härtet die lokale Persistenz gegen Offline-Betrieb, Reload und App-Update mit testgetriebener Absicherung.

Purpose: Verhindert Datenverlust und erfüllt die Haltbarkeitsanforderungen für lokale Nutzerdaten.
Output: RED→GREEN Persistenz-Regressionssuite (unit + component) und ggf. notwendige schema/repository Anpassungen.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/db/schema.ts
@src/repositories/meterReadingsRepository.ts
@src/repositories/pvDailyRepository.ts

<interfaces>
From existing repositories:
```ts
listNewestFirst(): Promise<...[]>;
save(input: ...): Promise<...>;
delete(id: string): Promise<void>;
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>task 1: RED — Persistenz-Gaps als fehlschlagende Regressionstests kodieren</name>
  <files>tests/unit/meterReadingsRepository.test.ts, tests/unit/pvDailyRepository.test.ts, tests/component/captureOfflinePersistence.test.ts</files>
  <action>Erstelle zunächst fehlschlagende Tests für drei Fälle: (a) Speichern ohne Netzverfügbarkeit im bestehenden local-first Flow, (b) Daten bleiben nach simuliertem Reload verfügbar, (c) Daten bleiben nach simuliertem Schema-/Versionsupgrade verfügbar. Keine Produktionsanpassung in diesem Schritt.</action>
  <verify>
    <automated>node --test tests/unit/meterReadingsRepository.test.ts tests/unit/pvDailyRepository.test.ts tests/component/captureOfflinePersistence.test.ts</automated>
  </verify>
  <done>Mindestens ein Test pro Fall schlägt reproduzierbar fehl und beschreibt den konkreten Nutzerrisiko-Fall.</done>
</task>

<task type="auto">
  <name>task 2: GREEN/REFACTOR — Repositories/Schema stabilisieren bis alle Persistenztests grün sind</name>
  <files>src/db/schema.ts, src/repositories/meterReadingsRepository.ts, src/repositories/pvDailyRepository.ts, src/repositories/settingsRepository.ts, tests/unit/settingsService.test.ts</files>
  <action>Implementiere die minimalen Anpassungen, damit alle RED-Fälle grün werden: saubere Dexie-Versionierung/Upgrade-Pfade, robuste Repository-Zugriffe und Sicherstellung, dass kein PWA-Update vorhandene Daten überschreibt. Refaktoriere nur bei vollständig grüner Suite.</action>
  <verify>
    <automated>node --test tests/unit/*.test.ts tests/component/*.test.ts</automated>
  </verify>
  <done>Offline-Speichern, Reload-Beständigkeit und Update-Beständigkeit sind automatisiert nachgewiesen.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SW update → IndexedDB schema | Neue App-Version trifft auf alte lokale Datenstruktur |
| UI input → local persistence | Untrusted Nutzerinput wird in lokale DB persistiert |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | T | Dexie upgrade path | mitigate | Versionierte, testgetriebene Migrationspfade mit Erhalt bestehender Records |
| T-04-04 | D | offline write flow | mitigate | Repository- und Component-Regressionstests für Save ohne Netzwerkabhängigkeit |
</threat_model>

<verification>
- node --test tests/unit/meterReadingsRepository.test.ts tests/unit/pvDailyRepository.test.ts tests/component/captureOfflinePersistence.test.ts
- node --test tests/unit/*.test.ts tests/component/*.test.ts
</verification>

<success_criteria>
- PWA-02/PWA-03 sind über automatisierte Persistenztests nachgewiesen.
- TEST-01/TEST-02 enthalten explizite Offline-/Reload-/Update-Regressionsfälle.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-offline-haertung/04-02-SUMMARY.md`
</output>
