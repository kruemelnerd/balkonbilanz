---
phase: 04-pwa-offline-haertung
plan: 04
type: execute
wave: 1
depends_on: [04-03]
files_modified:
  - tests/e2e/mobile-pwa-offline.playwright.spec.ts
  - tests/e2e/mobile-capture.playwright.spec.ts
  - tests/e2e/mobile-pwa-offline.spec.ts
  - package.json
autonomous: true
gap_closure: true
requirements: [PWA-01, PWA-04, TEST-03, TEST-05]
must_haves:
  truths:
    - "Browsernahe, mobile Offline-/Update-Flows sind automatisiert nachweisbar."
    - "Nutzer kann die App online laden, offline neu laden und weiterhin bedienen."
    - "Nutzer sieht den Update-Hinweis im realen Browserpfad und kann Reload ausloesen."
  artifacts:
    - path: "tests/e2e/mobile-pwa-offline.playwright.spec.ts"
      provides: "Echter mobiler Browser-Flow fuer Offline-Reload + Update-Hinweis"
    - path: "package.json"
      provides: "Playwright-Skript mit Phase-4-relevanten Specs"
  key_links:
    - from: "tests/e2e/mobile-pwa-offline.playwright.spec.ts"
      to: "src/main.ts"
      via: "echter App-Start im Browser statt Node-Mount"
      pattern: "page\.goto\(|context\.setOffline"
    - from: "tests/e2e/mobile-pwa-offline.playwright.spec.ts"
      to: "src/features/pwa/ReloadPrompt.vue"
      via: "sichtbarer Text 'Neue Version verfügbar'"
      pattern: "Neue Version verf[uü]gbar|Jetzt aktualisieren"
---

<objective>
Schliesst den Verifikations-Gap fuer echte mobile Browser-E2E-Abdeckung des PWA-Hauptflusses.

Purpose: TEST-03/TEST-05 und PWA-01/PWA-04 brauchen echten Browsernachweis statt Happy-DOM-Stubs.
Output: Ein realer Playwright-Offline/Update-Test und aktualisierte Playwright-Wiring-Skripte.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pwa-offline-haertung/04-VERIFICATION.md
@.planning/phases/04-pwa-offline-haertung/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — echte mobile PWA-Offline/Update-Erwartung als fehlschlagenden Playwright-Test festschreiben</name>
  <files>tests/e2e/mobile-pwa-offline.playwright.spec.ts</files>
  <behavior>
    - Test 1: App wird online geladen, danach Browser geht offline, Reload bleibt nutzbar (Capture-UI sichtbar).
    - Test 2: Update-Hinweis kann im realen Browserpfad sichtbar gemacht und mit "Jetzt aktualisieren" bestaetigt werden.
  </behavior>
  <action>Erstelle eine neue echte Playwright-Mobile-Spec (nicht `mountVueComponent`, nicht `fake-indexeddb`) mit Device-Emulation und `context.setOffline(true)`-Reload-Flow; Assertions muessen auf sichtbare Nutzerergebnisse pruefen. Wenn fuer Update-Hinweis ein deterministischer Trigger noetig ist, nutze nur browsernahe Hooks/Fakes im Testkontext (kein Rewrite der Produktlogik), strikt entsprechend Gap 1 aus 04-VERIFICATION.</action>
  <verify><automated>npx playwright test tests/e2e/mobile-pwa-offline.playwright.spec.ts --reporter=line</automated></verify>
  <done>Die neue Spec ist vorhanden und beschreibt den kompletten mobilen Offline-/Update-Hauptfluss als echten Browser-Test.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — Playwright-Wiring auf Phase-4-PWA-Hauptfluss umstellen</name>
  <files>package.json, tests/e2e/mobile-capture.playwright.spec.ts, tests/e2e/mobile-pwa-offline.spec.ts</files>
  <behavior>
    - Test 1: `npm run test:playwright` fuehrt Capture + neuen Offline-PWA-Browsertest aus.
    - Test 2: Keine irrefuehrende "E2E"-Benennung fuer Node/Happy-DOM-Tests verbleibt.
  </behavior>
  <action>Ergaenze `test:playwright` so, dass der neue Browser-Offline-Test ausgefuehrt wird; halte bestehenden Capture-Playwright-Test weiterhin aktiv. Bereinige/benenne die alte Node-basierte `mobile-pwa-offline.spec.ts` so, dass sie nicht mehr als browsernahe E2E missverstanden wird (per VERIFY-Gap-Anti-Pattern). Keine Scope-Erweiterung ueber die PWA-Phase hinaus.</action>
  <verify><automated>npm run test:playwright</automated></verify>
  <done>Playwright-Skript deckt den mobilen Offline/Update-Pfad real ab; keine falsche E2E-Semantik bleibt offen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser test harness → App runtime | Testdaten/Events simulieren untrusted Runtime-Zustaende (offline/update) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | tests/e2e/mobile-pwa-offline.playwright.spec.ts | mitigate | Nur oeffentliche UI-Signale und Browser-APIs verwenden; keine versteckten Produktiv-Backdoors in App-Code einfuehren. |
| T-04-02 | R | package.json test scripts | mitigate | Skript fix auf konkrete Spec-Pfade, damit Regressionsnachweis reproduzierbar bleibt. |
</threat_model>

<verification>
- npx playwright test tests/e2e/mobile-pwa-offline.playwright.spec.ts --reporter=line
- npm run test:playwright
</verification>

<success_criteria>
- Echte mobile Browser-Spec verifiziert Offline-Reload + Update-Hinweis fuer Phase 4.
- Playwright-Regressionspfad deckt Capture und PWA-Offline-Hauptfluss ab.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pwa-offline-haertung/04-04-SUMMARY.md`
</output>
