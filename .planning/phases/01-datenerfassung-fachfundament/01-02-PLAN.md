---
phase: 01-datenerfassung-fachfundament
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/domain/validation/meterValidation.ts
  - src/domain/validation/pvValidation.ts
  - src/services/meterReadingService.ts
  - src/services/pvDailyService.ts
autonomous: true
requirements:
  - METER-04
  - METER-05
  - METER-06
  - PV-01
  - PV-02
  - PV-03
user_setup: []
must_haves:
  truths:
    - Invalid meter and PV inputs are blocked with explicit reasons.
    - Duplicate meter timestamps are rejected deterministically.
    - Decreasing meter values trigger block or meter-change flow.
    - PV writes for today/future are rejected and same-day updates use upsert.
  artifacts:
    - src/domain/validation/meterValidation.ts
    - src/domain/validation/pvValidation.ts
    - src/services/meterReadingService.ts
    - src/services/pvDailyService.ts
  key_links:
    - Services call validators before repository writes.
---

<objective>
Implement hard domain validation and write orchestration services.

Purpose: Enforce non-negotiable business rules consistently across all entry points.
Output: Pure validators and service-layer flows that block or route invalid data.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenerfassung-fachfundament/01-CONTEXT.md
@.planning/phases/01-datenerfassung-fachfundament/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>task 1: Build pure meter validation module</name>
  <files>src/domain/validation/meterValidation.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-datenerfassung-fachfundament/01-CONTEXT.md</read_first>
  <action>Implement required-field, numeric format, non-negative, duplicate timestamp, and monotonic progression checks with typed error results suitable for UI mapping.</action>
  <verify>Unit tests cover valid and invalid branches for each METER rule.</verify>
  <acceptance_criteria>
    - validator returns machine-readable reason codes per failed rule
    - duplicate timestamp and descending values are explicitly detectable
  </acceptance_criteria>
  <done>Meter validator behavior is deterministic and fully testable.</done>
</task>

<task type="auto">
  <name>task 2: Build pure PV day validation module</name>
  <files>src/domain/validation/pvValidation.ts</files>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Implement calendar-day validation that allows only completed past days, validates value formatting/ranges, and supports update intent for existing day rows.</action>
  <verify>Unit tests include timezone-boundary and today/future rejection cases.</verify>
  <acceptance_criteria>
    - validator blocks today and future dates
    - validator supports edit/upsert use case for same day
  </acceptance_criteria>
  <done>PV validator safely enforces day-based capture rules.</done>
</task>

<task type="auto">
  <name>task 3: Wire services to validators and repositories</name>
  <files>src/services/meterReadingService.ts, src/services/pvDailyService.ts</files>
  <read_first>src/domain/validation/meterValidation.ts, src/domain/validation/pvValidation.ts, src/repositories/meterReadingsRepository.ts, src/repositories/pvDailyRepository.ts</read_first>
  <action>Create service methods for create/update/delete that normalize input, call validators, and only persist when validation passes; implement explicit meter-change routing contract.</action>
  <verify>Service tests prove invalid requests never perform repository writes.</verify>
  <acceptance_criteria>
    - service returns structured failure responses for UI error rendering
    - repository write calls happen only on successful validation
  </acceptance_criteria>
  <done>Service layer becomes the single write gateway for Phase-1 entities.</done>
</task>
</tasks>

<verification>
- [ ] Unit tests for validation modules and services
- [ ] TypeScript build/type-check passes
- [ ] Rule-to-requirement traceability check for METER-04..06 and PV-01..03
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Hard validation behavior is consistently enforced before persistence
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenerfassung-fachfundament/01-02-SUMMARY.md`
</output>
