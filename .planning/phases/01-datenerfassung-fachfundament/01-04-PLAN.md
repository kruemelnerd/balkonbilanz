---
phase: 01-datenerfassung-fachfundament
plan: 04
type: execute
wave: 4
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - src/db/database.ts
  - src/db/schema.ts
  - src/repositories/meterReadingsRepository.ts
  - src/repositories/pvDailyRepository.ts
  - src/stores/captureStore.ts
  - src/features/capture/CaptureView.vue
  - src/features/meter/MeterEntryForm.vue
  - src/features/meter/MeterReadingsList.vue
  - src/features/pv/PvDailyForm.vue
  - src/features/pv/PvDailyList.vue
  - src/main.ts
  - src/App.vue
autonomous: true
requirements:
  - METER-01
  - METER-02
  - METER-03
  - METER-04
  - METER-05
  - METER-06
  - PV-01
  - PV-02
  - PV-03
  - PV-04
  - PV-05
user_setup: []
must_haves:
  truths:
    - "Nutzer kann Zaehlerablesungen mit Zeitpunkt sowie OBIS 1.8.0/2.8.0 in der laufenden App erfassen, bearbeiten und loeschen."
    - "Nutzer kann PV-Tageswerte nur fuer vergangene Tage erfassen/aktualisieren und als Tagesliste sehen."
    - "Fachliche Blockierungen (Pflichtfelder, Format, negativ, doppelter Zeitpunkt, sinkende Zaehlerwerte) sind in der UI sichtbar und verhindern Persistenz."
    - "Zaehler- und PV-Daten werden in IndexedDB (Dexie) persistiert und bleiben nach Reload erhalten."
  artifacts:
    - path: src/db/database.ts
      provides: konkrete Dexie-DB-Klasse und Tabellenverdrahtung
    - path: src/features/capture/CaptureView.vue
      provides: produktive Shell fuer Meter/PV-Erfassungsflows
    - path: src/main.ts
      provides: produktiver App-Einstieg mit Renderpfad
  key_links:
    - from: src/features/capture/CaptureView.vue
      to: src/stores/captureStore.ts
      via: createCaptureStore(...) und reactive binding
      pattern: createCaptureStore
    - from: src/stores/captureStore.ts
      to: src/db/database.ts
      via: konkrete Repository-Instanzen statt Test-Stubs
      pattern: new BalkonBilanzDb
---

<objective>
Schliesst die Verifikationsluecken bei produktiver Verdrahtung und lokaler Persistenz fuer Phase 01.

Purpose: Die vorhandene Fachlogik wird in eine reale, bedienbare App ueberfuehrt statt nur in Test-Stubs.
Output: Laufender Capture-Screen (Meter + PV), echte Dexie-Instanz, persistente CRUD-Fluesse mit UI-Fehlerblockierung.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenerfassung-fachfundament/01-CONTEXT.md
@.planning/phases/01-datenerfassung-fachfundament/01-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Verdrahte echte lokale Dexie-Persistenz fuer Meter und PV</name>
  <files>src/db/database.ts, src/db/schema.ts, src/repositories/meterReadingsRepository.ts, src/repositories/pvDailyRepository.ts</files>
  <read_first>src/db/schema.ts, src/repositories/meterReadingsRepository.ts, src/repositories/pvDailyRepository.ts, .planning/phases/01-datenerfassung-fachfundament/01-VERIFICATION.md</read_first>
  <behavior>
    - Test 1: Ein gespeicherter Meter-Datensatz ist nach erneuter Repository-Instanz weiter abrufbar.
    - Test 2: PV-Upsert aktualisiert denselben Kalendertag statt Duplikat zu erzeugen.
  </behavior>
  <action>Erzeuge `BalkonBilanzDb` mit Dexie-Tabellen und binde bestehende Repository-Factorys produktiv an diese DB an (kein In-Memory-Adapter im App-Pfad). Behalte Ereignismodell fuer Meter und Tagesmodell fuer PV strikt getrennt (D-01, D-06, D-08). Stelle sicher, dass Schreibpfade die vorhandenen Validierungs-/Service-Gates weiter nutzen und nicht an UI vorbei direkt persistieren (D-03, D-07).</action>
  <verify>
    <automated>node --test tests/unit/meterReadingsRepository.test.ts tests/unit/pvDailyRepository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/database.ts` exportiert eine konkrete Dexie-DB-Klasse (`BalkonBilanzDb`) mit Meter- und PV-Tabellen.
    - Repository-Instanzen im App-Pfad verwenden die konkrete Dexie-DB (keine In-Memory-Implementierung im produktiven Pfad).
    - `node --test tests/unit/meterReadingsRepository.test.ts tests/unit/pvDailyRepository.test.ts` endet erfolgreich.
  </acceptance_criteria>
  <done>Produktiver Datenpfad nutzt Dexie/IndexedDB; Meter/PV bleiben nach Reload konsistent verfuegbar.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Erzeuge produktive Capture-Shell fuer Meter/PV inklusive Fehler- und Zaehlerwechsel-Flow</name>
  <files>src/features/capture/CaptureView.vue, src/stores/captureStore.ts, src/features/meter/MeterEntryForm.vue, src/features/meter/MeterReadingsList.vue, src/features/pv/PvDailyForm.vue, src/features/pv/PvDailyList.vue</files>
  <read_first>src/stores/captureStore.ts, src/features/meter/MeterEntryForm.vue, src/features/pv/PvDailyForm.vue, src/services/meterReadingService.ts, src/services/pvDailyService.ts</read_first>
  <behavior>
    - Test 1: Unerlaubte Meter-/PV-Eingaben bleiben blockiert und zeigen klare Fachfehlermeldungen.
    - Test 2: Sinkende Zaehlerwerte zeigen den dokumentierten `meter-change-required`-Pfad oder blockieren hart.
  </behavior>
  <action>Baue eine produktive View, die den Capture-Store erstellt, Meter- und PV-Komponenten rendert und CRUD-Endpunkte sichtbar bedient. Fehlertexte muessen fachlich klar und handlungsorientiert sein (D-10). Implementiere fuer sinkende Zaehlerwerte einen fuer Nutzer erreichbaren, dokumentierten Flow (Banner + explizite Folgeaktion) oder harte Blockierung gem. D-04; keine stille Korrektur. Listen bleiben mobile-first, neueste zuerst, mit schnellen Bearbeiten/Loeschen-Aktionen (D-02, D-09).</action>
  <verify>
    <automated>node --test tests/unit/meterReadingService.test.ts tests/unit/pvDailyService.test.ts tests/unit/captureServiceEdgeCases.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/capture/CaptureView.vue` rendert Meter- und PV-Capture-Komponenten im produktiven App-Pfad.
    - Fehlerfaelle (Pflicht/Format/negativ/duplicate/today-future) bleiben blockierend und sichtbar in der UI.
    - Sinkende Zaehlerwerte fuehren zu dokumentierter Blockierung oder `meter-change-required`-Flow (keine stille Korrektur).
  </acceptance_criteria>
  <done>Die App hat einen echten, erreichbaren Capture-Nutzerpfad fuer Meter und PV mit wirksamer Fehlerblockierung.</done>
</task>

<task type="auto">
  <name>task 3: Bootstrapping der App fuer produktive Nutzung der Capture-View</name>
  <files>src/main.ts, src/App.vue</files>
  <read_first>src/main.ts, src/App.vue, src/features/capture/CaptureView.vue</read_first>
  <action>Lege den produktiven Vue-Einstieg an und render die Capture-View als Startscreen, damit Meter/PV-Flows nicht nur in Tests existieren. Halte die Struktur spaeter erweiterbar fuer Phase-2-Routen, aber ohne Scope-Ausweitung. Der Startpfad muss unmittelbar die in D-01 bis D-10 geforderten Erfassungsflows erreichbar machen.</action>
  <verify>
    <automated>node --test tests/unit/*.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/main.ts` mountet die produktive App ohne Test-Harness.
    - `src/App.vue` bindet die Capture-View als erreichbaren Startscreen ein.
    - `node --test tests/unit/*.test.ts` bleibt erfolgreich nach dem Bootstrapping.
  </acceptance_criteria>
  <done>App startet in einer laufenden Shell, in der die Capture-Features tatsaechlich bedienbar sind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> Vue form state | Untrusted Eingaben (Zeitpunkt, OBIS, PV-kWh, Notiz) gelangen in Validierungslogik |
| UI/store -> repository/db | Nur validierte Daten duerfen IndexedDB erreichen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | T | captureStore submit-Pfade | mitigate | Vor jedem Write ausschliesslich ueber Services mit `validateMeterReading`/`validatePvDailyEntry`; keine direkten Repo-Writes aus Komponenten |
| T-01-04-02 | R | meter-change-required Flow | mitigate | Persistentes, pruefbares UI-Signal + dokumentierte Folgeaktion, damit kritische Ablehnungen nicht "verschwinden" |
| T-01-04-03 | D | IndexedDB corruption / schema mismatch | mitigate | Versionierte Dexie-DB-Klasse mit klaren Table-Schemas und defensivem DB-Init-Handling |
| T-01-04-04 | I | Fehlertexte | accept | Nur fachliche Eingabefehler ohne PII; kein externer Datenabfluss |
</threat_model>

<verification>
- Dexie-basierte Repository-Tests bestehen
- Service-/Validierungs-Tests fuer Blockierungsregeln bestehen
- Produktiver App-Einstieg rendert Capture-View (kein reiner Testpfad)
</verification>

<success_criteria>
- Verifikationsgaps zu "ORPHANED/HOLLOW" Artefakten sind geschlossen
- Meter- und PV-Flows laufen in der echten App inklusive CRUD + Validierungsblockierung
- Lokale Persistenz ist produktiv angebunden und reload-stabil
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenerfassung-fachfundament/01-04-SUMMARY.md`
</output>
