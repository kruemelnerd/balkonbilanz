---
phase: 01-datenerfassung-fachfundament
plan: 06
type: execute
wave: 6
depends_on:
  - 01-05
files_modified:
  - vite.config.ts
autonomous: true
gap_closure: true
requirements:
  - METER-01
  - PV-01
user_setup: []
must_haves:
  truths:
    - "App startet im Cold-Start ohne Vite Import-Analyse-Fehler und Hauptansicht ist erreichbar."
    - "Vue-SFCs (inkl. src/App.vue) werden von Vite korrekt geparst."
  artifacts:
    - path: vite.config.ts
      provides: Registrierung von @vitejs/plugin-vue fuer SFC-Transformation
    - path: src/App.vue
      provides: bestehender SFC-Einstieg, der im Startup korrekt geladen wird
  key_links:
    - from: vite.config.ts
      to: src/App.vue
      via: plugins: [vue()] in Vite-Pipeline
      pattern: "plugins:\\s*\\[\\s*vue\\(\\)\\s*\\]"
---

<objective>
Schliesst den UAT-Blocker aus 01-UAT Test 1 durch eine minimale, zielgenaue Vite-Konfiguration, damit die bestehende Capture-App wieder startbar ist.

Purpose: Ohne funktionsfaehigen Vue-SFC-Startup sind alle fachlichen Phase-1-Validierungen blockiert.
Output: Vite-Konfiguration mit aktivem Vue-Plugin plus reproduzierbarer Cold-Start-/SFC-Smoke-Nachweis.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenerfassung-fachfundament/01-UAT.md
@.planning/phases/01-datenerfassung-fachfundament/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Registriere Vue-SFC-Transformation in Vite</name>
  <files>vite.config.ts</files>
  <read_first>vite.config.ts, src/main.ts, src/App.vue, .planning/phases/01-datenerfassung-fachfundament/01-UAT.md</read_first>
  <action>Lege im Projektroot eine `vite.config.ts` an (oder erweitere die bestehende Datei), importiere `defineConfig` aus `vite` sowie `vue` aus `@vitejs/plugin-vue` und registriere `plugins: [vue()]`. Keine weiteren Build-/Alias-/PWA-Aenderungen in diesem Gap-Fix vornehmen (minimaler Root-Cause-Fix). Dadurch wird der Startup-Fehler fuer `src/App.vue` behoben und die in Phase 1 umgesetzten Flows (D-01 bis D-10) wieder ausfuehrbar. Dabei D-05 explizit wahren: keine Soft-Warnlogik fuer nur auffaellige, aber logisch moegliche Werte in Phase 1 einfuehren oder aendern.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` importiert `vue` aus `@vitejs/plugin-vue` und registriert `plugins: [vue()]`.
    - `npm run build` endet ohne `[plugin:vite:import-analysis]` Fehler fuer `.vue`-Dateien.
    - Es werden keine zusaetzlichen Alias-/PWA-/Build-Mutationen ausser dem Plugin-Fix eingefuehrt.
  </acceptance_criteria>
  <done>Build laeuft ohne Import-Analyse-Fehler zu `.vue`-Dateien; `vite.config.ts` enthaelt aktive Vue-Plugin-Registrierung; D-05 bleibt unveraendert (keine neuen Soft-Warnregeln in Phase 1).</done>
</task>

<task type="auto">
  <name>task 2: Fuehre Cold-Start und Vue-SFC-Startup-Smoke erneut aus</name>
  <files>vite.config.ts</files>
  <read_first>vite.config.ts, src/main.ts, src/App.vue</read_first>
  <action>Validiere gezielt den gemeldeten UAT-Blocker mit Startup-Fokus: starte Vite im Dev-Modus und pruefe, dass kein Fehleroverlay "Install @vitejs/plugin-vue" mehr auftritt und die Hauptansicht erreichbar bleibt. Halte die Verifikation eng am Gap (kein breiter Re-Test aller Phase-1-Faelle in diesem Plan).</action>
  <verify>
    <automated>npm run dev -- --host 127.0.0.1 --port 4173 --strictPort &amp; DEV_PID=$!; sleep 8; curl -fsS http://127.0.0.1:4173 &gt; /tmp/phase01-06-home.html; kill $DEV_PID; wait $DEV_PID 2&gt;/dev/null || true; rg -q "&lt;div id=\"app\"" /tmp/phase01-06-home.html</automated>
  </verify>
  <acceptance_criteria>
    - Der Dev-Server startet auf `127.0.0.1:4173` ohne Vue-Plugin-Hinweisfehler.
    - `curl -fsS http://127.0.0.1:4173` liefert HTTP 200 waehrend laufendem Server.
    - Die Antwort enthaelt `<div id="app"` als messbaren Startup-Nachweis.
  </acceptance_criteria>
  <done>Cold-Start gelingt stabil; `src/App.vue` wird als SFC verarbeitet und die App startet bis zur Hauptansicht ohne Parse-Abbruch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source -> Vite transform pipeline | Unkonfigurierte Build-Pipeline kann valide Vue-SFCs als ungueltiges JS behandeln |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | D | App startup pipeline | mitigate | `@vitejs/plugin-vue` explizit in `vite.config.ts` registrieren, damit Startup nicht durch Parser-Fehler blockiert wird |
| T-01-06-02 | T | Build configuration | mitigate | Scope auf minimalen Root-Cause-Fix begrenzen; keine weiteren Konfigurationsmutationen im selben Plan |
| T-01-06-03 | R | UAT evidence | mitigate | Cold-Start-Smoke nach Fix erneut ausfuehren und reproduzierbar im Summary dokumentieren |
</threat_model>

<verification>
- `npm run build` endet ohne `[plugin:vite:import-analysis]` Fehler auf `.vue`
- HTTP-Check auf `http://127.0.0.1:4173` liefert 200 (`curl -fsS`) waehrend laufendem Dev-Server
- Response enthält App-Root (`&lt;div id="app"`) als messbare Assertion fuer "Hauptansicht erreichbar"
</verification>

<success_criteria>
- Der in 01-UAT diagnostizierte Blocker (Test 1) ist technisch beseitigt.
- Vue-SFC-Startup ist wiederhergestellt; nachgelagerte UAT-Tests sind nicht mehr durch App-Start blockiert.
- Fix bleibt minimal und betrifft nur die Build-Pipeline-Ursache.
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenerfassung-fachfundament/01-06-SUMMARY.md`
</output>
