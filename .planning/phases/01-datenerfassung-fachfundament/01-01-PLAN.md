---
phase: 01-datenerfassung-fachfundament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - src/repositories/meterReadingsRepository.ts
  - src/repositories/pvDailyRepository.ts
  - src/domain/types.ts
autonomous: true
requirements:
  - METER-01
  - METER-02
  - METER-03
  - PV-04
  - PV-05
user_setup: []
must_haves:
  truths:
    - Meter events and PV day values persist locally with stable structure.
    - Meter events support create/update/delete and ordered retrieval.
    - PV day entries support list and single-entry-per-day upsert behavior.
  artifacts:
    - src/db/schema.ts
    - src/repositories/meterReadingsRepository.ts
    - src/repositories/pvDailyRepository.ts
  key_links:
    - Repositories use shared typed domain models from src/domain/types.ts.
---

<objective>
Create the local persistence foundation for meter events and PV day values.

Purpose: Establish durable data model and repository operations needed by all Phase-1 flows.
Output: Dexie schema plus typed repositories for meter and PV entities.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenerfassung-fachfundament/01-CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>task 1: Define storage schema and canonical entity types</name>
  <files>src/db/schema.ts, src/domain/types.ts</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create Dexie tables for meter readings and PV day entries, with indexes on timestamp/date and typed domain models that distinguish event-based meter data from day-based PV data.</action>
  <verify>Run TypeScript check and verify exported interfaces/types compile.</verify>
  <acceptance_criteria>
    - schema file declares two dedicated tables for meter and PV data
    - domain types encode timestamp for meter and date-only for PV
  </acceptance_criteria>
  <done>Schema and type exports are consumable by repository modules.</done>
</task>

<task type="auto">
  <name>task 2: Implement meter repository CRUD and sorted retrieval</name>
  <files>src/repositories/meterReadingsRepository.ts</files>
  <read_first>src/db/schema.ts, src/domain/types.ts</read_first>
  <action>Implement create/update/delete/get/list operations with deterministic newest-first ordering and helper query by exact timestamp for uniqueness checks.</action>
  <verify>Repository unit tests for CRUD and ordering pass.</verify>
  <acceptance_criteria>
    - repository exposes CRUD plus listNewestFirst API
    - timestamp lookup method exists for duplicate detection workflows
  </acceptance_criteria>
  <done>Meter repository is ready for validation/service integration.</done>
</task>

<task type="auto">
  <name>task 3: Implement PV daily repository with day-upsert semantics</name>
  <files>src/repositories/pvDailyRepository.ts</files>
  <read_first>src/db/schema.ts, src/domain/types.ts</read_first>
  <action>Implement insert-or-update by day key, list by day descending, and delete/update helpers aligned with one-entry-per-day rule.</action>
  <verify>Repository unit tests confirm no duplicate day entries are created.</verify>
  <acceptance_criteria>
    - upsert-by-day API updates existing day instead of creating duplicate
    - list returns date/value/note/source fields for UI consumption
  </acceptance_criteria>
  <done>PV repository enforces single record per calendar day.</done>
</task>
</tasks>

<verification>
- [ ] Unit tests for repository CRUD and ordering
- [ ] TypeScript build/type-check passes
- [ ] Manual data roundtrip check in local IndexedDB
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Persistence foundation supports all Phase-1 input flows
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenerfassung-fachfundament/01-01-SUMMARY.md`
</output>
