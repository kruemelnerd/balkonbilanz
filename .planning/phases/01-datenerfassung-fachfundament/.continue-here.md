---
context: phase
phase: 01-datenerfassung-fachfundament
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-05-11T08:55:06.101Z
---

# BLOCKING CONSTRAINTS - read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Slash command invocation path - direct shell execution of `/gsd-*` is not reliable in this environment - run GSD steps via workflow/orchestrator invocation.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Direct slash command as shell binary | ` /gsd-plan-phase 1 --auto` failed when invoked as raw shell command. | advisory | Execute GSD workflow commands through orchestrated command path/tooling, not direct shell path. |

**Severity values:** `blocking` - resuming agent must pass understanding check before proceeding. `advisory` - important context, does not gate resumption.

<current_state>
Phase 1 (Datenerfassung & Fachfundament) is fully discussed and planned. No implementation tasks have started yet. Branch is `gsd/phase-01-datenerfassung-fachfundament` and execution is ready to begin with plan `01-01` task 1.
</current_state>

<completed_work>

Completed Tasks:
- task 1: Phase 1 context captured (`01-CONTEXT.md`) - Done
- task 2: Phase 1 discussion log captured (`01-DISCUSSION-LOG.md`) - Done
- task 3: Phase 1 research + three execution plans created (`01-RESEARCH.md`, `01-01-PLAN.md`, `01-02-PLAN.md`, `01-03-PLAN.md`) - Done
- task 4: GSD model profile switched to `genius` (planning/execution/verification model mapping written) - Done (uncommitted files)
</completed_work>

<remaining_work>

- task 1 (01-01): Define Dexie schema and canonical domain types (`src/db/schema.ts`, `src/domain/types.ts`)
- task 2 (01-01): Implement meter repository CRUD + ordered retrieval (`src/repositories/meterReadingsRepository.ts`)
- task 3 (01-01): Implement PV daily repository with day-upsert semantics (`src/repositories/pvDailyRepository.ts`)
- task 4 (phase continuation): Execute plan `01-02` (validation + services)
- task 5 (phase continuation): Execute plan `01-03` (UI flows + unit/component/e2e/gherkin coverage)
</remaining_work>

<decisions_made>

- Decided to keep meter data event-based with exact timestamps and separate OBIS totals because this matches real meter behavior and avoids false day-level assumptions.
- Chose strict blocking validation (instead of warn-and-save) because requirements METER-04..06 and PV-01..03 require deterministic data integrity.
- Chose one PV entry per day with upsert for same day because PV is modeled as completed-day values only.
- Chose phase ordering capture -> analysis -> advisor/backup -> PWA because early service-worker caching would hide core logic defects.
</decisions_made>

<blockers>
- Workflow invocation constraint: direct shell execution of `/gsd-*` may fail in this runtime; use workflow/orchestrator invocation path. Workaround known and documented.
</blockers>

## Required Reading (in order)
1. `.planning/phases/01-datenerfassung-fachfundament/01-CONTEXT.md` - locked decisions D-01..D-10 that must not drift.
2. `.planning/phases/01-datenerfassung-fachfundament/01-RESEARCH.md` - phase-specific implementation patterns and pitfalls.
3. `.planning/phases/01-datenerfassung-fachfundament/01-01-PLAN.md` - immediate next execution steps.
4. `.planning/REQUIREMENTS.md` - authoritative requirement IDs for traceability (METER-01..06, PV-01..05).
5. `.planning/PROJECT.md` - core value and architectural constraints (local-first, strict domain logic).

## Critical Anti-Patterns (do NOT repeat these)
- DIRECT-SLASH-COMMAND-AS-SHELL: Calling `/gsd-*` directly in shell can fail -> invoke through the intended workflow/orchestrator pathway.

## Infrastructure State
- Git branch: `gsd/phase-01-datenerfassung-fachfundament`
- Running services/watchers: none
- Uncommitted files currently present:
  - `AGENTS.md`
  - `.planning/oc_config.json`
  - `opencode.json`
  - `.opencode/`
  - `.idea/`
  - `balkonbilanz_spec_driven_development.md`

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/01-datenerfassung-fachfundament/01-01-PLAN.md`, `.planning/phases/01-datenerfassung-fachfundament/01-02-PLAN.md`, `.planning/phases/01-datenerfassung-fachfundament/01-03-PLAN.md`
- Critique focus: verify task ordering really enforces repository-before-service-before-UI and that each requirement maps to executable tests (unit + component + e2e + gherkin).
- Gate: Do NOT begin execution until critique is complete and any plan corrections are applied.

<context>
The project is in a clean planning-to-execution transition point: all strategic artifacts exist, and no production code has been created yet. The highest leverage now is disciplined execution of 01-01 with TDD-first increments and strict separation of domain rules from UI.
</context>

<next_action>
Start with: run `/gsd-execute-phase 1`, begin `01-01` task 1 by creating `src/db/schema.ts` and `src/domain/types.ts`, then immediately add/green unit checks for type/schema contracts.
</next_action>
