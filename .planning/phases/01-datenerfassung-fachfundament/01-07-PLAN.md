---
phase: 01-datenerfassung-fachfundament
plan: 07
type: execute
wave: 7
depends_on: ["01-06"]
files_modified:
  - src/stores/captureStore.ts
  - tests/component/meterCapture.test.ts
  - tests/unit/captureStore.test.ts
autonomous: true
requirements: [METER-03, METER-01]
gap_closure: true
must_haves:
  truths:
    - "Beim Bearbeiten eines Zaehlerstands ist das Zeitpunkt-Feld im datetime-local-kompatiblen Format vorbefuellt (ohne Browser-Warnung)."
    - "Der Meter-Edit-Flow bleibt speicherbar und validierbar auf Mobile-Viewports."
  artifacts:
    - path: "src/stores/captureStore.ts"
      provides: "Konvertierung von gespeichertem ISO-Zeitstempel in datetime-local-Format fuer Edit-Prefill"
      contains: "startMeterEdit"
    - path: "tests/unit/captureStore.test.ts"
      provides: "Automatisierter Nachweis fuer datetime-local-kompatibles Prefill"
      contains: "startMeterEdit"
    - path: "tests/component/meterCapture.test.ts"
      provides: "Rendered-Flow-Nachweis fuer Edit-Prefill ohne Warnformat"
      contains: "meter capture flow"
  key_links:
    - from: "src/stores/captureStore.ts"
      to: "src/features/meter/MeterEntryForm.vue"
      via: "meter.draft.timestamp -> input[type=datetime-local]"
      pattern: "timestamp"
---

<objective>
Schliesst den verbleibenden Human-UAT-Gap zum Meter-Edit-Prefill, damit der reale Mobile-Edit-Flow ohne ungueltiges datetime-local-Format funktioniert.

Purpose: Korrigiert den dokumentierten UAT-Root-Cause in `01-HUMAN-UAT.md` ohne Scope-Creep.
Output: Korrigierte Prefill-Transformation plus automatisierte Testabdeckung fuer den Edit-Fall.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-datenerfassung-fachfundament/01-HUMAN-UAT.md
@src/stores/captureStore.ts
@src/features/meter/MeterEntryForm.vue
@tests/component/meterCapture.test.ts

<interfaces>
From src/stores/captureStore.ts:
```ts
export interface MeterDraftState {
  timestamp: string;
  obis180Kwh: string;
  obis280Kwh: string;
  note: string;
}

export interface CaptureStore {
  startMeterEdit(id: number): Promise<boolean>;
  updateMeterDraft(patch: Partial<MeterDraftState>): void;
  submitMeter(): Promise<MeterReadingServiceResult<MeterReadingRecord>>;
}
```

From src/features/meter/MeterEntryForm.vue:
```vue
<input id="meter-timestamp" v-model="props.store.meter.draft.timestamp" type="datetime-local" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — failing tests fuer datetime-local Edit-Prefill erstellen</name>
  <files>tests/unit/captureStore.test.ts, tests/component/meterCapture.test.ts</files>
  <read_first>src/stores/captureStore.ts, src/features/meter/MeterEntryForm.vue, tests/unit/captureStore.test.ts, tests/component/meterCapture.test.ts, .planning/phases/01-datenerfassung-fachfundament/01-HUMAN-UAT.md</read_first>
  <behavior>
    - Test 1: `startMeterEdit()` fuellt `meter.draft.timestamp` als `YYYY-MM-DDTHH:mm` ohne Sekunden und ohne `Z` (per D-02, D-09).
    - Test 2: Der gerenderte Capture-Flow zeigt nach Edit-Start einen datetime-local-kompatiblen Prefillwert statt ISO-Zeitstempel mit `Z` (per D-10).
  </behavior>
  <action>Ergaenze/erstelle Tests so, dass sie gegen den aktuellen Stand reproduzierbar fehlschlagen: gespeicherter ISO-Record wird editiert und der erwartete Prefill ist explizit lokal/datetime-local-kompatibel; keine Akzeptanz fuer rohe ISO-UTC-Strings mit `Z`.</action>
  <verify>
    <automated>node --test tests/unit/captureStore.test.ts tests/component/meterCapture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mindestens ein Test faellt initial reproduzierbar wegen ISO-`Z` Prefill im Edit-Flow.
    - Der erwartete Prefill ist explizit als `YYYY-MM-DDTHH:mm` ohne Sekunden und ohne `Z` formuliert.
    - Die fehlende datetime-local-Kompatibilitaet ist als konkrete Regression dokumentiert.
  </acceptance_criteria>
  <done>Mindestens ein neuer/angepasster Test ist zunaechst rot und beschreibt den UAT-Gap praezise.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN/REFACTOR — Edit-Prefill-Konvertierung im Store implementieren</name>
  <files>src/stores/captureStore.ts, tests/unit/captureStore.test.ts, tests/component/meterCapture.test.ts</files>
  <read_first>src/stores/captureStore.ts, src/domain/types.ts, tests/unit/captureStore.test.ts, tests/component/meterCapture.test.ts</read_first>
  <behavior>
    - Gespeicherte Meter-Timestamps bleiben intern ISO-normalisiert fuer Persistenz/Validierung.
    - Beim Start des Edit-Flows wird nur der Formular-Prefill in datetime-local-kompatibles lokales Format transformiert.
    - Bestehende Meter-CRUD- und Validierungsfluesse bleiben unveraendert gruene Regressionen.
  </behavior>
  <action>Implementiere eine explizite Prefill-Transformation in `toMeterDraft()` bzw. einem dedizierten Helper in `captureStore.ts`, der ISO-Recordwerte fuer `input[type=datetime-local]` auf `YYYY-MM-DDTHH:mm` abbildet (per D-01, D-02). Keine Aenderung an Persistenzschema, Service-Contracts oder PV-Flow. Passe Tests aus task 1 auf gruen und refaktoriere klein, ohne den Scope ueber den UAT-Root-Cause hinaus zu erweitern.</action>
  <verify>
    <automated>node --test tests/unit/*.test.ts tests/component/*.test.ts tests/e2e/mobile-capture.spec.ts tests/bdd/steps/capture.steps.ts</automated>
  </verify>
  <acceptance_criteria>
    - `startMeterEdit`/`toMeterDraft` liefert fuer Edit-Prefill datetime-local-kompatibles `YYYY-MM-DDTHH:mm`.
    - Persistenzpfad bleibt ISO-normalisiert; keine Schema- oder Service-Contract-Aenderung.
    - Regression-Suite fuer Unit/Component/E2E/BDD bleibt vollstaendig gruen.
  </acceptance_criteria>
  <done>Editieren eines Meter-Eintrags setzt einen browser-kompatiblen datetime-local-Prefill ohne Warnformat; alle relevanten automatisierten Capture-Checks sind gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI form input -> store edit draft | Untrusted form values and persisted timestamps are rendered into input controls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | T (Tampering) | `src/stores/captureStore.ts` timestamp prefill mapping | mitigate | Keep persistence path unchanged and restrict conversion to edit-prefill formatting only; cover with unit test asserting output format. |
| T-01-07-02 | D (DoS) | `MeterEntryForm.vue` datetime-local input | mitigate | Prevent invalid timestamp format injection (`...Z`) from store into field; add rendered-flow assertion in component test. |
| T-01-07-03 | R (Repudiation) | Meter edit flow | accept | No identity/auth scope in local-only app; change tracking remains out of Phase-1 scope and is documented. |
</threat_model>

<verification>
- UAT-gap regression: `node --test tests/unit/captureStore.test.ts tests/component/meterCapture.test.ts`
- Full capture safety net: `node --test tests/unit/*.test.ts tests/component/*.test.ts tests/e2e/mobile-capture.spec.ts tests/bdd/steps/capture.steps.ts`
</verification>

<success_criteria>
- Der in `01-HUMAN-UAT.md` dokumentierte Gap zum datetime-local-Prefill ist im Code geschlossen.
- Meter-Edit auf Mobile verursacht keine ungueltige `datetime-local`-Vorfuellung mehr.
- Die bestehenden Phase-1-Anforderungen bleiben vollstaendig erfuellt (keine Regression bei METER/PV-Flows).
</success_criteria>

<output>
After completion, create `.planning/phases/01-datenerfassung-fachfundament/01-07-SUMMARY.md`
</output>
