---
phase: 02-analyse-dashboard-datenqualitaet
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/analysis/intervalTypes.ts
  - src/domain/analysis/calculateMeterIntervals.ts
  - src/domain/analysis/calculateCombinedKpis.ts
  - src/domain/analysis/evaluateDataQuality.ts
  - tests/unit/analysisIntervals.test.ts
  - tests/unit/analysisCombinedKpis.test.ts
  - tests/unit/analysisQuality.test.ts
  - tests/bdd/analysis-quality.feature
  - tests/bdd/steps/analysisQuality.steps.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, ANLY-03, ANLY-04, QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "Intervallausgaben enthalten Start, Ende und Dauer je aufeinanderfolgender Ablesung"
    - "Kosten werden intervallweise berechnet und zeigen ehrlichen Fallback statt Scheingenauigkeit"
    - "Kombinierte KPIs sind als Naeherung modelliert und liefern Qualitaetslevel mit Gruenden"
  artifacts:
    - path: "src/domain/analysis/calculateMeterIntervals.ts"
      provides: "Intervall- und kWh/Tag-Berechnung inkl. Warnflags"
    - path: "src/domain/analysis/evaluateDataQuality.ts"
      provides: "good/limited/poor mit konkreten Reason-Codes"
    - path: "src/domain/analysis/calculateCombinedKpis.ts"
      provides: "Schaetz-KPIs inkl. PV<Einspeisung-Plausibilitaetsflag"
  key_links:
    - from: "calculateMeterIntervals"
      to: "calculateCombinedKpis"
      via: "interval aggregate input"
      pattern: "importKwh|exportKwh|duration"
    - from: "evaluateDataQuality"
      to: "combined KPI presentation"
      via: "quality level + reasons"
      pattern: "good|limited|poor"
---

<objective>
Domain-Engine fuer Analyse/Kosten/Qualitaet als TDD-Grundlage implementieren.

Purpose: Die geschaeftskritische Rechenlogik wird vor UI-Wiring deterministisch und testbar stabilisiert.
Output: Pure Functions + Unit/Bdd-Orakel fuer Intervalle, Kosten-Fallback, Naeherungs-KPIs und Datenqualitaet.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-RESEARCH.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
@balkonbilanz_spec_driven_development.md

<interfaces>
From src/domain/types.ts:
```ts
export interface MeterReadingRecord { timestamp: MeterTimestamp; obis180Kwh: number; obis280Kwh: number; }
export interface PvDailyRecord { day: PvDay; generationKwh: number; source: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — Analyse-Domain-Contracts und fehlschlagende Tests definieren</name>
  <read_first>
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
    src/domain/types.ts
    tests/unit/captureServiceEdgeCases.test.ts
  </read_first>
  <files>src/domain/analysis/intervalTypes.ts, tests/unit/analysisIntervals.test.ts, tests/unit/analysisCombinedKpis.test.ts, tests/unit/analysisQuality.test.ts, tests/bdd/analysis-quality.feature, tests/bdd/steps/analysisQuality.steps.ts</files>
  <behavior>
    - Test 1: Intervall aus zwei Ablesungen liefert Start/Ende/Dauer und kWh-Deltas (INT-01/02, per D-12 neueste zuerst).
    - Test 2: kWh/Tag wird aus realer Dauer berechnet und bei ungueltiger Dauer nicht als normalisierter Wert ausgewiesen (INT-03).
    - Test 3: Kostenbasis verwendet 0.305 EUR/kWh als Default je Intervall, aber liefert status `unavailable` + UI-Text "Kosten noch nicht verfuegbar", wenn keine belastbare Basis gegeben ist (INT-04, per D-13..D-16).
    - Test 4: Plausibilitaetsflag fuer auffaellige Spruenge und fuer PV<Einspeisung (INT-05, QUAL-03, per D-08).
    - Test 5: Qualitaetsmodell liefert good/limited/poor mit Gruenden fuer >7-Tage-Intervalle und PV-Vollstaendigkeitsquote (QUAL-01/02, per D-01..D-03).
  </behavior>
  <action>Lege die Analyse-Contract-Typen (`MeterIntervalResult`, `IntervalCostResult`, `CombinedKpiResult`, `DataQualityResult`) an und schreibe zuerst fehlschlagende Unit-Tests + ein deutsches Gherkin-Feature fuer Qualitaets-/Naeherungsverhalten gemaess D-01 bis D-16. Keine Implementierungslogik in den neuen Domain-Dateien vor dem ersten roten Lauf.</action>
  <verify>
    <automated>node --test tests/unit/analysisIntervals.test.ts tests/unit/analysisCombinedKpis.test.ts tests/unit/analysisQuality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Der Testlauf endet mit mindestens einem FAIL in jeder der drei neuen Unit-Dateien.
    - `tests/bdd/analysis-quality.feature` enthaelt die Woerter `Naeherung`, `good`, `limited`, `poor` und `Kosten noch nicht verfuegbar`.
    - `src/domain/analysis/intervalTypes.ts` exportiert exakt `MeterIntervalResult`, `IntervalCostResult`, `CombinedKpiResult`, `DataQualityResult`.
  </acceptance_criteria>
  <done>Fehlschlagende Spezifikation fuer Intervall-, Kosten-, KPI- und Qualitaetslogik liegt vollstaendig vor.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — Intervall-, Kosten- und Qualitaetslogik implementieren</name>
  <read_first>
    src/domain/analysis/intervalTypes.ts
    tests/unit/analysisIntervals.test.ts
    tests/unit/analysisCombinedKpis.test.ts
    tests/unit/analysisQuality.test.ts
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
  </read_first>
  <files>src/domain/analysis/calculateMeterIntervals.ts, src/domain/analysis/calculateCombinedKpis.ts, src/domain/analysis/evaluateDataQuality.ts</files>
  <action>Implementiere pure Functions entsprechend den RED-Tests: Intervallbildung aus sortierten Ablesungen (neueste zuerst per D-12), kWh/Tag-Normalisierung, intervallfaehige Kostenaufloesung mit Standardpreis `0.305` per D-13 und ehrlichem `unavailable` per D-16, neutrales Naeherungsmodell fuer kombinierte KPIs per D-05..D-07, sowie Qualitaetsbewertung mit 7-Tage-Regel und PV-Vollstaendigkeitsquote inkl. konkreten Reason-Codes per D-01..D-03.</action>
  <verify>
    <automated>node --test tests/unit/analysisIntervals.test.ts tests/unit/analysisCombinedKpis.test.ts tests/unit/analysisQuality.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Alle drei Unit-Testdateien laufen gruen.
    - `evaluateDataQuality.ts` enthaelt explizite Schwellwertpruefung fuer Intervalllaenge `> 7` Tage.
    - `calculateMeterIntervals.ts` enthaelt einen Rueckgabepfad mit Kennzeichnung `costStatus: 'unavailable'`.
    - `calculateCombinedKpis.ts` enthaelt Plausibilitaetspruefung fuer `pvTotalKwh < exportTotalKwh`.
  </acceptance_criteria>
  <done>Rechenlogik ist deterministisch, testbar und deckt alle gelockten Fachentscheidungen D-01 bis D-16 ab.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: REFACTOR — Domain-API stabilisieren und BDD-Schritte synchronisieren</name>
  <read_first>
    src/domain/analysis/calculateMeterIntervals.ts
    src/domain/analysis/calculateCombinedKpis.ts
    src/domain/analysis/evaluateDataQuality.ts
    tests/bdd/analysis-quality.feature
  </read_first>
  <files>src/domain/analysis/calculateMeterIntervals.ts, src/domain/analysis/calculateCombinedKpis.ts, src/domain/analysis/evaluateDataQuality.ts, tests/bdd/steps/analysisQuality.steps.ts</files>
  <action>Fuehre nur strukturierende Refactors ohne Verhaltensaenderung durch: duplizierte Guards extrahieren, Reason-Code-Mapping zentralisieren, sprechende Funktionsnamen setzen und BDD-Step-Implementierung auf die finalen Domain-Funktionssignaturen ausrichten. Keine Logik in Vue-Komponenten verschieben.</action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` ist gruen.
    - Die drei Domain-Dateien enthalten keine Vue-Imports (`from 'vue'` darf nicht vorkommen).
    - `tests/bdd/steps/analysisQuality.steps.ts` importiert mindestens eine neue Analysefunktion aus `src/domain/analysis/`.
  </acceptance_criteria>
  <done>Stabile Domain-API fuer nachfolgende Store/UI-Plans ist bereitgestellt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eingabeformular→Domain-Engine | Untrusted numerische/zeitliche Werte beeinflussen KPI-Berechnung |
| Domain-Output→UI | Berechnete Schaetzwerte koennen als exakte Fakten missverstanden werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | calculateMeterIntervals | mitigate | Harte Guards fuer negative/ungueltige Dauer und nicht-numerische Werte; Tests fuer Invalid-Faelle |
| T-02-02 | I | calculateCombinedKpis | mitigate | Erzwinge Labels/Flags `isEstimate` + Grundtexte, damit keine Scheingenauigkeit entsteht |
| T-02-03 | T | evaluateDataQuality | mitigate | Deterministische Schwellenregeln (>7 Tage, PV-Abdeckung) inkl. Reason-Codes statt impliziter Einstufung |
| T-02-04 | D | Kostenauflosung | mitigate | Fehlende Tarifbasis liefert `unavailable` statt `0 EUR`, verhindert fachliche Fehlinterpretation |
</threat_model>

<verification>
Domain-Regression: `npm run test:unit`
</verification>

<success_criteria>
Alle Phase-2-Domain-Bausteine fuer INT/ANLY/QUAL sind testbar, deterministisch und als Naeherung transparent modelliert.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyse-dashboard-datenqualitaet/02-01-SUMMARY.md`
</output>
