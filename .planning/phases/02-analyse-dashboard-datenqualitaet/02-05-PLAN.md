---
phase: 02-analyse-dashboard-datenqualitaet
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - src/features/analysis/AnalysisView.vue
  - src/features/analysis/IntervalList.vue
  - src/features/analysis/analysisCopy.ts
  - src/features/dashboard/DashboardView.vue
  - src/features/capture/CaptureView.vue
  - tests/component/appShellNavRouter.test.ts
  - tests/component/dashboardView.test.ts
  - tests/component/analysisViewStates.test.ts
  - tests/component/intervalList.test.ts
  - tests/bdd/analysis-dashboard.feature
  - tests/bdd/steps/analysisDashboard.steps.ts
  - tests/e2e/mobile-analysis.spec.ts
autonomous: true
gap_closure: true
requirements:
  - INT-05
  - DASH-01
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "Nutzer sieht Plausibilitaetswarnungen als deutsche, erklaerende Hinweise statt Raw-Codes."
    - "Nutzer sieht konkrete Datenqualitaetsgruende konsistent in Dashboard und Analyse."
    - "Dashboard-Schnellaktionen fuehren direkt in die passende Erfassung statt dekorativ zu bleiben."
  artifacts:
    - path: "src/features/analysis/analysisCopy.ts"
      provides: "Zentrales Mapping von Quality-Reason-Codes, Combined-Warnings und Interval-Flags auf deutsche UI-Texte"
    - path: "src/features/analysis/IntervalList.vue"
      provides: "Sichtbare Inline-Plausibilitaetswarnungen fuer Intervall-Flags wie suspicious_jump"
    - path: "src/features/dashboard/DashboardView.vue"
      provides: "Verdrahtete Schnellaktionen und wiederverwendete Datenqualitaets-Texte"
    - path: "src/features/capture/CaptureView.vue"
      provides: "Hash-/Fokus-Ziele fuer Meter- und PV-Quick-Actions"
  key_links:
    - from: "analysisCopy.ts"
      to: "AnalysisView.vue / DashboardView.vue / IntervalList.vue"
      via: "shared German text mapping"
      pattern: "pv_below_export|pv_coverage_partial|interval_over_7_days|suspicious_jump"
    - from: "DashboardView.vue"
      to: "CaptureView.vue"
      via: "router push or RouterLink to /capture#meter-timestamp and /capture#pv-day"
      pattern: "/capture#meter-timestamp|/capture#pv-day"
---

<objective>
Schliesst die in `02-VERIFICATION.md` dokumentierten Live-Luecken bei Warn-/Qualitaetskommunikation und Dashboard-Schnellaktionen mit minimalem Scope-Creep.

Purpose: Die Berechnungen sind vorhanden, aber Endnutzer sehen aktuell technische Codes oder gar keine Aktion; dieser Plan vervollstaendigt die letzte Meile zur nachvollziehbaren Live-UX.
Output: Gemeinsame deutsche Copy-Mappings, sichtbare Intervall-Warnhinweise, verdrahtete Quick-Actions und belastbare Regressionstests ueber Component/BDD/E2E.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-VERIFICATION.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-03-PLAN.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-04-PLAN.md
@src/features/capture/CaptureView.vue
@src/features/meter/MeterEntryForm.vue
@src/features/pv/PvDailyForm.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: RED — Live-Wiring-Luecken mit echten Raw-Codes und Quick-Action-Navigation reproduzierbar rot machen</name>
  <read_first>
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-VERIFICATION.md
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
    src/features/analysis/AnalysisView.vue
    src/features/analysis/IntervalList.vue
    src/features/dashboard/DashboardView.vue
    src/features/capture/CaptureView.vue
    tests/component/dashboardView.test.ts
    tests/component/analysisViewStates.test.ts
    tests/component/intervalList.test.ts
    tests/component/appShellNavRouter.test.ts
  </read_first>
  <files>tests/component/dashboardView.test.ts, tests/component/analysisViewStates.test.ts, tests/component/intervalList.test.ts, tests/component/appShellNavRouter.test.ts, tests/bdd/analysis-dashboard.feature, tests/bdd/steps/analysisDashboard.steps.ts, tests/e2e/mobile-analysis.spec.ts</files>
  <action>Ersetze die aktuell zu wohlwollenden Snapshot-/Textannahmen durch fehlschlagende Live-Wiring-Checks: uebergib in Tests bewusst Raw-Codes wie `pv_below_export`, `pv_coverage_partial`, `interval_over_7_days` und das Intervall-Flag `suspicious_jump`; erwarte stattdessen exakte deutsche Nutztexte wie `Plausibilitaetswarnung: Einspeisung liegt ueber dem erfassten PV-Tagesertrag.`, `Nur 3 von 7 PV-Tagen vorhanden` und einen sichtbaren Hinweis fuer auffaellige Zaehlerspruenge. Erweitere Router-/Dashboard-Tests so, dass Klicks auf `Zaehlerstand erfassen` nach `/capture#meter-timestamp` und auf `PV-Tageswert erfassen` nach `/capture#pv-day` fuehren; E2E/BDD muessen dieselben Flows und Copy-Regeln aus Nutzersicht beschreiben.</action>
  <verify>
    <automated>node --test tests/component/appShellNavRouter.test.ts tests/component/dashboardView.test.ts tests/component/analysisViewStates.test.ts tests/component/intervalList.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Mindestens ein Test faellt rot, solange `AnalysisView.vue` noch `pv_below_export` oder `quality.reasons` direkt rendert.
    - Mindestens ein Test faellt rot, solange `IntervalList.vue` keine sichtbaren Hinweise fuer `flags` rendert.
    - Mindestens ein Test faellt rot, solange beide Dashboard-Buttons keinen Route- oder Fokus-Effekt ausloesen.
    - `tests/bdd/analysis-dashboard.feature` enthaelt die exakten Texte `Nur 3 von 7 PV-Tagen vorhanden`, `Plausibilitaetswarnung` und `Zaehlerstand erfassen`.
  </acceptance_criteria>
  <done>Die dokumentierten Verifikationsluecken sind als reproduzierbar rote Spezifikation eingefroren.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: GREEN — Gemeinsame deutsche Copy-Mappings, Intervall-Flags und Quick-Actions produktiv verdrahten</name>
  <read_first>
    src/features/analysis/AnalysisView.vue
    src/features/analysis/IntervalList.vue
    src/features/dashboard/DashboardView.vue
    src/features/capture/CaptureView.vue
    src/features/meter/MeterEntryForm.vue
    src/features/pv/PvDailyForm.vue
    tests/component/appShellNavRouter.test.ts
    tests/component/analysisViewStates.test.ts
  </read_first>
  <files>src/features/analysis/analysisCopy.ts, src/features/analysis/AnalysisView.vue, src/features/analysis/IntervalList.vue, src/features/dashboard/DashboardView.vue, src/features/capture/CaptureView.vue</files>
  <action>Fuehre ein gemeinsames Mapping-Modul `src/features/analysis/analysisCopy.ts` ein und exportiere dort konkrete Funktionen fuer drei Faelle: (1) `pv_below_export` -> `Plausibilitaetswarnung: Einspeisung liegt ueber dem erfassten PV-Tagesertrag.`, (2) Quality-Reasons wie `pv_coverage_partial`, `interval_over_7_days`, `minimal_basis` -> deutsche Erklaerungen mit Zahlenbezug, (3) Intervall-Flags wie `suspicious_jump` und `pv_export_mismatch` -> persistente Inline-Hinweise pro Intervall. Nutze dieses Mapping in `AnalysisView.vue` und `DashboardView.vue`, sodass beide Views dieselben deutschen Gruende zeigen und niemals Raw-Codes rendern. Erweitere `IntervalList.vue` um sichtbare Warnkarten/Listen pro Intervall auf Basis von `interval.flags`. Verdrahte in `DashboardView.vue` beide Schnellaktionen konkret auf `/capture#meter-timestamp` bzw. `/capture#pv-day`; ergaenze in `CaptureView.vue` das minimale Hash-/Fokus-Wiring, damit nach Navigation das jeweilige Eingabefeld sichtbar und fokussierbar ist, ohne Phase-1-CRUD-Logik anzufassen.</action>
  <verify>
    <automated>node --test tests/component/appShellNavRouter.test.ts tests/component/dashboardView.test.ts tests/component/analysisViewStates.test.ts tests/component/intervalList.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/features/analysis/analysisCopy.ts` exportiert klar benannte Mapping-Funktionen fuer Quality-Reasons, Combined-Warnings und Interval-Flags.
    - `AnalysisView.vue` rendert nicht mehr direkt `combined.warnings[0]` oder rohe `quality.reasons` in die UI.
    - `DashboardView.vue` rendert nicht mehr rohe `qualityReasons`, sondern die gemeinsame deutsche Copy.
    - `IntervalList.vue` enthaelt sichtbares Rendering fuer mindestens `suspicious_jump` und `pv_export_mismatch`.
    - `DashboardView.vue` enthaelt verdrahtete Schnellaktionen nach `/capture#meter-timestamp` und `/capture#pv-day`.
    - `CaptureView.vue` enthaelt fokussierbares Hash-Ziel fuer `meter-timestamp` und `pv-day`.
  </acceptance_criteria>
  <done>Die Nutzer sehen fachlich lesbare Warnungen/Gruende und koennen aus dem Dashboard direkt in die passende Erfassung springen.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: REFACTOR — Regressionen ueber BDD und mobilen Dashboard→Capture→Analyse-Flow absichern</name>
  <read_first>
    tests/bdd/analysis-dashboard.feature
    tests/bdd/steps/analysisDashboard.steps.ts
    tests/e2e/mobile-analysis.spec.ts
    src/features/dashboard/DashboardView.vue
    src/features/analysis/analysisCopy.ts
  </read_first>
  <files>tests/bdd/analysis-dashboard.feature, tests/bdd/steps/analysisDashboard.steps.ts, tests/e2e/mobile-analysis.spec.ts</files>
  <action>Schliesse nach gruener Implementierung die letzte Testluecke: aktualisiere das BDD-Feature so, dass Warn-/Qualitaetscopy und beide Quick-Actions in deutscher Fachsprache beschrieben werden; erweitere den mobilen Smoke-Test um Klicks auf beide Dashboard-Schnellaktionen, Route-Assertion auf `/capture#meter-timestamp` bzw. `/capture#pv-day` oder das korrespondierende fokussierte Feld sowie um einen Nachweis, dass Analyse keine Raw-Codes wie `pv_below_export` oder `pv_coverage_partial` mehr zeigt. Halte den Scope eng auf die in `02-VERIFICATION.md` genannten Gaps.</action>
  <verify>
    <automated>node --test tests/bdd/steps/analysisDashboard.steps.ts tests/e2e/mobile-analysis.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `analysis-dashboard.feature` enthaelt mindestens ein Szenario fuer Quick-Actions und eines fuer lesbare Warn-/Qualitaetsgruende.
    - `mobile-analysis.spec.ts` prueft beide Dashboard-Schnellaktionen gegen unterschiedliche Capture-Ziele.
    - Kein neuer oder aktualisierter Test akzeptiert die Raw-Codes `pv_below_export`, `pv_coverage_partial` oder `interval_over_7_days` als sichtbare Endnutzertexte.
  </acceptance_criteria>
  <done>Die Gap-Fixes sind auf allen geforderten Testebenen abgesichert und koennen erneut verifiziert werden.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain-/Store-Codes -> User-facing copy | Technische Codes duerfen nicht ungefiltert in produktive Nutzertexte durchrutschen |
| Dashboard CTA -> Capture entry points | Sichtbare Aktionen duerfen nicht ins Leere laufen oder den falschen Fokus setzen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | I | Analysis/Dashboard warning copy | mitigate | Zentrales Mapping verhindert technische Codes als sichtbare Nutzerkommunikation |
| T-02-15 | T | IntervalList warning rendering | mitigate | Persistente Inline-Hinweise pro `interval.flags` sichern Plausibilitaetskontext ohne Daten zu blockieren |
| T-02-16 | D | Dashboard quick actions | mitigate | Router-verdrahtete Capture-Ziele mit Fokus-Assertion verhindern dekorative, wirkungslose Buttons |
</threat_model>

<verification>
- `node --test tests/component/appShellNavRouter.test.ts tests/component/dashboardView.test.ts tests/component/analysisViewStates.test.ts tests/component/intervalList.test.ts`
- `node --test tests/bdd/steps/analysisDashboard.steps.ts tests/e2e/mobile-analysis.spec.ts`
- `npm run build`
</verification>

<success_criteria>
- Die drei in `02-VERIFICATION.md` dokumentierten Blocker sind konkret adressiert.
- Live-UI zeigt deutsche Warn-/Qualitaetskommunikation statt technischer Codes.
- Dashboard-Schnellaktionen fuehren reproduzierbar in die passende Meter- bzw. PV-Erfassung.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyse-dashboard-datenqualitaet/02-05-SUMMARY.md`
</output>
