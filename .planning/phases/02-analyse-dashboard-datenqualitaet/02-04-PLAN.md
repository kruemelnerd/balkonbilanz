---
phase: 02-analyse-dashboard-datenqualitaet
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - tests/e2e/mobile-analysis.spec.ts
  - tests/bdd/analysis-dashboard.feature
  - tests/bdd/steps/analysisDashboard.steps.ts
  - tests/component/analysisViewStates.test.ts
  - tests/unit/analysisRegression.test.ts
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, DASH-01, DASH-02, ANLY-01, ANLY-02, ANLY-03, ANLY-04, QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "Hauptfluss Dashboard→Analyse ist mobil testbar"
    - "Gherkin-Szenarien decken Naeherung, Qualitaet und Plausibilitaetswarnung ab"
    - "Regressionstests sichern lokale Persistenz und Router/Store-Wiring"
  artifacts:
    - path: "tests/bdd/analysis-dashboard.feature"
      provides: "Akzeptanzkriterien in deutscher Fachsprache"
    - path: "tests/e2e/mobile-analysis.spec.ts"
      provides: "mobiler End-to-End-Fluss fuer Phase-2-Funktionen"
    - path: "tests/component/analysisViewStates.test.ts"
      provides: "Loading/Empty/Partial/Error/Warning State-Abdeckung"
  key_links:
    - from: "analysis-dashboard.feature"
      to: "analysisDashboard.steps.ts"
      via: "step definitions"
      pattern: "Naeherung|good|limited|poor"
    - from: "mobile-analysis.spec.ts"
      to: "router + analysis views"
      via: "navigation and assertions"
      pattern: "Dashboard|Erfassung|Analyse"
---

<objective>
Verpflichtende Testpyramide fuer Phase 2 finalisieren (Unit, Component, UI/E2E, Gherkin).

Purpose: Fachlogik- und UI-Verhalten werden gegen Regressionen abgesichert und AGENTS-Qualitaetsvorgaben nachweisbar erfuellt.
Output: Ausfuehrbare Tests fuer mobile Analysefluesse, Zustandsdarstellung und Qualitaets-/Naeherungsregeln.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@balkonbilanz_spec_driven_development.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
@tests/e2e/mobile-capture.spec.ts
@tests/bdd/meter-capture.feature
@tests/bdd/steps/capture.steps.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Gherkin-Akzeptanz fuer Dashboard/Analyse und Qualitaetslogik erstellen</name>
  <read_first>
    balkonbilanz_spec_driven_development.md
    tests/bdd/meter-capture.feature
    tests/bdd/steps/capture.steps.ts
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
  </read_first>
  <files>tests/bdd/analysis-dashboard.feature, tests/bdd/steps/analysisDashboard.steps.ts</files>
  <action>Erstelle ein deutsches BDD-Feature mit Szenarien fuer: Dashboard leer/befuellt, Analyse-Default 30 Tage + Presets 7/30/90, getrennte PV-Tageswerte, Naeherungslabel, Quality-Level mit Gruenden sowie persistente Plausibilitaetswarnung bei `PV < Einspeisung`. Step-Definitions als node:test-kompatibles Scaffolding analog Capture anlegen.</action>
  <verify>
    <automated>node --test tests/bdd/steps/analysisDashboard.steps.ts</automated>
  </verify>
  <acceptance_criteria>
    - `analysis-dashboard.feature` enthaelt mindestens 5 `Szenario:`-Bloecke.
    - Feature-Datei enthaelt die Texte `Naeherung`, `good`, `limited`, `poor` und `Plausibilitaetswarnung`.
    - Step-Datei ist ohne Syntaxfehler per `node --test` ausfuehrbar.
  </acceptance_criteria>
  <done>Fachliche Akzeptanz fuer alle Phase-2-Kernanforderungen ist ausfuehrbar spezifiziert.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Component- und Unit-Regression fuer Analyse-Zustaende und Persistenz-Wiring</name>
  <read_first>
    src/features/analysis/AnalysisView.vue
    src/features/dashboard/DashboardView.vue
    src/stores/analysisStore.ts
    tests/component/meterCapture.test.ts
  </read_first>
  <files>tests/component/analysisViewStates.test.ts, tests/unit/analysisRegression.test.ts</files>
  <action>Erweitere die Testabdeckung fuer State-Contract aus UI-SPEC: Loading-Skeleton, Empty, Partial-data (abgegraute KPI bei `poor`), Warning-Karte, Error mit `Erneut laden`. Ergaenze Unit-Regression auf lokale Persistenz/Wiring (Store laedt nach Reload dieselben Analysegrunddaten aus Repositories, keine Router-State-Verluste bei View-Wechsel).</action>
  <verify>
    <automated>node --test tests/component/analysisViewStates.test.ts tests/unit/analysisRegression.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Component-Test prueft explizit die Strings `Erneut laden` und `Kosten noch nicht verfuegbar`.
    - Mindestens ein Test validiert abgegraute Darstellung bei `poor` (z. B. CSS-Klasse oder aria-Attribut).
    - Unit-Regression enthaelt einen Reload-Pfad mit konsistenter Ergebnislaenge vor/nach Reload.
  </acceptance_criteria>
  <done>Zustands- und Persistenzrisiken sind durch reproduzierbare Tests abgesichert.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Mobiler End-to-End-Smoke fuer Dashboard→Analyse erstellen</name>
  <read_first>
    tests/e2e/mobile-capture.spec.ts
    src/router/index.ts
    src/features/shell/AppShellNav.vue
    src/features/analysis/AnalysisView.vue
  </read_first>
  <files>tests/e2e/mobile-analysis.spec.ts</files>
  <action>Erstelle einen mobilen Smoke-Test nach existierendem Muster: Navigation ueber Bottom-Nav zwischen `Dashboard`, `Erfassung`, `Analyse`; Validierung des 30-Tage-Defaults; Umschalten auf Preset `7 Tage`; Sichtbarkeit von Intervallsektion, PV-Tageswert-Sektion und Naeherungs-/Qualitaetshinweisen. Test muss ohne externe Requests auskommen (lokale Datenfixtures/seed nutzen).</action>
  <verify>
    <automated>node --test tests/e2e/mobile-analysis.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Testdatei enthaelt Assertions fuer alle drei Nav-Labels.
    - Test enthaelt Assertion auf `30 Tage` beim Initialzustand.
    - Test enthaelt Assertion auf `7 Tage` nach Preset-Wechsel.
    - Test enthaelt Assertion auf mindestens eines der Woerter `Naeherung` oder `Schaetzung`.
  </acceptance_criteria>
  <done>Mobiler Hauptfluss der Phase 2 ist automatisiert verifiziert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures→Domain/UI | Schlechte Fixtures koennen falsche Sicherheit erzeugen |
| Router/store reload→local persistence | Regressionsrisiko bei Einfuehrung neuer App-Shell-Strukturen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | T | analysis fixtures/tests | mitigate | Fixture-Sets mit expliziten PV-Luecken, langen Intervallen und PV<Einspeisung als Pflichtfaelle |
| T-02-12 | I | BDD + component assertions | mitigate | Assertions auf konkrete Warn-/Naeherungs-Texte verhindern stilles Entfernen der Risiko-Hinweise |
| T-02-13 | D | router/store persistence | mitigate | Regressionstest fuer Reload + View-Wechsel, damit keine Datenansicht durch Stateverlust ausfaellt |
</threat_model>

<verification>
`npm test`
</verification>

<success_criteria>
Alle Phase-2-Anforderungen sind ueber mindestens eine automatisierte Ebene (Unit/Component/Bdd/E2E) verifiziert und gegen Regression abgesichert.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyse-dashboard-datenqualitaet/02-04-SUMMARY.md`
</output>
