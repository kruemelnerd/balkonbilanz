---
context: phase
phase: 02-analyse-dashboard-datenqualitaet
task: 2
total_tasks: 3
status: paused
last_updated: 2026-05-11T20:14:18.790Z
---

# BLOCKING CONSTRAINTS - read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Slash command invocation path - direct shell execution of `/gsd-*` is not reliable in this environment - run GSD steps via workflow/orchestrator invocation.
- [ ] CONSTRAINT: Gap execution order - `/gsd-execute-phase 1` can replay the last completed plan when a diagnosed UAT gap has no fresh gap plan yet - run `/gsd-plan-phase <phase> --gaps` first, then `/gsd-execute-phase <phase> --gaps-only`.
- [ ] CONSTRAINT: Shell `rg` availability - this shell does not provide `rg`; use OpenCode `grep`/`glob` tools or a bash fallback that does not depend on `rg`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| DIRECT-SLASH-COMMAND-AS-SHELL | Raw shell execution of `/gsd-*` commands is unreliable in this environment. | advisory | Invoke slash workflows through the orchestrated command path/tooling, not as direct shell binaries. |
| EXECUTE-GAPS-WITHOUT-GAP-PLAN | Running execute directly after a diagnosed UAT gap can just echo the previous completed plan if no new gap plan exists. | advisory | When UAT reports a gap, create a gap plan first with `/gsd-plan-phase <phase> --gaps`, then execute with `--gaps-only`. |
| ASSUME-RG-IN-SHELL | Workflow snippets that assume `rg` exists can fail mid-run in this environment. | advisory | Prefer OpenCode search tools; if a shell command is required, avoid hard dependence on `rg`. |

**Severity values:** `blocking` - resuming agent must pass understanding check before proceeding. `advisory` - important context, does not gate resumption.

<current_state>
Phase 1 is functionally closed: the `01-07` gap fix converted meter edit prefills to a `datetime-local`-compatible local format, and `01-HUMAN-UAT.md` is now `complete` with both checks passing. Phase 2 has started at the design-contract level only: `02-UI-SPEC.md` was created and approved, but there is still no `02-CONTEXT.md` and no Phase-2 plan artifacts yet. The branch is already `gsd/phase-02-analyse-dashboard-datenqualitaet`, so the next action belongs to Phase 2 context/planning, not Phase 1 execution.
</current_state>

<completed_work>

Completed Tasks:
- task 1: Erstelle und genehmige den UI-Design-Contract fuer Phase 2 - Done (`5068516`)
- task 2: Plane und schliesse den Phase-1-Gap fuer datetime-local Meter-Edit-Prefill - Done (`4deeb3a`, `837d978`, `cf93f61`, `441ec9d`)
- task 3: Verifiziere Phase 1 per Human-UAT erneut - Done (Artefakt `01-HUMAN-UAT.md` aktualisiert, aktuell noch uncommitted im dirty Worktree)
</completed_work>

<remaining_work>

- task 2: Starte `/gsd-discuss-phase 2 /home/philipp/Dokumente/code/balkonbilanz_new` und erzeuge `02-CONTEXT.md` auf Basis des verpflichtenden UI-SPECs
- task 3: Fuehre `/gsd-plan-phase 2 /home/philipp/Dokumente/code/balkonbilanz_new` aus, sobald die Diskussion/Kontextsammlung steht
- hygiene: Entscheide, welche bereits vorhandenen uncommitted Dateien zu spaeteren atomaren Commits gehoeren; `STATE.md` und `01-UAT.md` koennen vom tatsaechlichen Stand abweichen
</remaining_work>

<decisions_made>

- Phase 2 erfordert verpflichtend `/gsd-ui-phase 2` vor weiterer Diskussion oder Planung.
- Phase 2 bleibt bei einem kleinen manuellen CSS-Designsystem mit Bottom-Navigation, KPI-Hero, Datenqualitaetskarte und ehrlicher `Naeherung`-/`Schaetzung`-Sprache.
- Beim Meter-Edit-Prefill wird nur der Formularwert ins lokale `datetime-local`-Format konvertiert; persistierte ISO-Zeitstempel bleiben unveraendert.
</decisions_made>

<blockers>
- Kein Produkt-Blocker.
- Prozesshygiene: `STATE.md` zeigt noch Phase 01 als executing, und das legacy-Artefakt `01-UAT.md` kann noch alte Cold-start-Skips enthalten. Verlasse dich zuerst auf `01-HUMAN-UAT.md`, `01-07-SUMMARY.md` und `02-UI-SPEC.md`.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` - machine-readable resume state used by `/gsd-resume-work`.
2. `.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md` - approved UI contract and mandatory input before any further Phase-2 discussion/planning.
3. `.planning/ROADMAP.md` - Phase-2 goals, requirement IDs, and milestone sequencing.
4. `.planning/STATE.md` - project snapshot, but verify it against current artifacts because it drifts.
5. `.planning/phases/01-datenerfassung-fachfundament/01-HUMAN-UAT.md` - proof that Phase 1 human UAT is now complete.
6. `.planning/phases/01-datenerfassung-fachfundament/01-07-SUMMARY.md` - exact rationale and test coverage for the last Phase-1 defect fix.
7. `.planning/METHODOLOGY.md` (if it exists) - project analytical lenses; apply before any assumption analysis.

## Critical Anti-Patterns (do NOT repeat these)
- DIRECT-SLASH-COMMAND-AS-SHELL: Calling `/gsd-*` directly in shell can fail -> invoke through intended workflow/orchestrator path.
- EXECUTE-GAPS-WITHOUT-GAP-PLAN: Diagnosed UAT gaps need a fresh gap plan first -> run `/gsd-plan-phase <phase> --gaps` before `--gaps-only` execution.
- ASSUME-RG-IN-SHELL: Shell snippets depending on `rg` can fail here -> prefer OpenCode search tools or a plain-shell fallback.

## Infrastructure State
- Git branch: `gsd/phase-02-analyse-dashboard-datenqualitaet`
- Running services/watchers: none known
- Worktree is intentionally dirty across planning, tooling, and test files; do not auto-revert unrelated changes
- Placeholder scan found stale `TBD` text in `.planning/phases/01-datenerfassung-fachfundament/.continue-here.md` under an old critique placeholder

## Pre-Execution Critique Required
- Design artifact: `.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md`
- Critique focus: verify that approximation labeling, quality explanations, dashboard KPI prioritization, and the strict separation of meter intervals vs. PV day values remain traceable to Phase-2 requirements before planning/execution starts
- Gate: Do NOT begin Phase-2 implementation until discussion/context and plan artifacts align with the approved UI contract

<context>
This pause happens exactly at the transition from a re-verified Phase 1 into pre-planning for Phase 2. The most important mental model is that Phase 2 is now UI-contract-first by explicit decision: the UI-SPEC already exists and is mandatory context, so resume should not redo UI direction or jump straight into code. First discuss/contextualize Phase 2 with the UI contract in hand, then plan the execution slices. Keep the dirty worktree intact until each file's ownership is understood.
</context>

<next_action>
Start with: run `/gsd-discuss-phase 2 /home/philipp/Dokumente/code/balkonbilanz_new`, treating `02-UI-SPEC.md` as a required input, then continue with `/gsd-plan-phase 2 /home/philipp/Dokumente/code/balkonbilanz_new`.
</next_action>
