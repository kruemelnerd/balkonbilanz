---
phase: 02-analyse-dashboard-datenqualitaet
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/services/analysis/analysisService.ts
  - src/stores/analysisStore.ts
  - src/features/analysis/AnalysisRangeCard.vue
  - src/features/analysis/IntervalList.vue
  - src/features/analysis/PvDaySummaryList.vue
  - tests/unit/analysisStore.test.ts
  - tests/component/analysisRangeCard.test.ts
  - tests/component/intervalList.test.ts
autonomous: true
requirements: [ANLY-01, ANLY-02, INT-01, INT-02, INT-03, INT-04, INT-05]
must_haves:
  truths:
    - "Analyse startet mit letzten 30 Tagen und bietet Presets 7/30/90"
    - "Filter-Reset setzt explizit auf den 30-Tage-Standard zurueck"
    - "Intervalle und PV-Tageswerte werden getrennt dargestellt"
  artifacts:
    - path: "src/stores/analysisStore.ts"
      provides: "Zeitraumzustand, Load-Status und derived Analyse-Output"
    - path: "src/services/analysis/analysisService.ts"
      provides: "Orchestrierung Domain-Funktionen + Zeitraumfilter"
    - path: "src/features/analysis/PvDaySummaryList.vue"
      provides: "PV-Tageswerte als separater Block"
  key_links:
    - from: "analysisStore"
      to: "analysisService"
      via: "loadAnalysis(range)"
      pattern: "reset.*30|preset"
    - from: "AnalysisRangeCard"
      to: "analysisStore"
      via: "preset and reset actions"
      pattern: "7|30|90"
---

<objective>
Analyse-State, Zeitraumsteuerung und getrennte Ergebnisdarstellung implementieren.

Purpose: Nutzer koennen den Zeitraum steuern und verifizierbare Intervall-/PV-Ausgaben erhalten, ohne Datentypen zu vermischen.
Output: Analysis-Service + Store + Zeitraum-/Intervall-/PV-Komponenten mit automatisierten Tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-01-PLAN.md
@src/stores/captureStore.ts
</context>

<tasks>

  <task type="auto" tdd="true">
  <name>task 1: Analysis-Service und Store mit 30-Tage-Default aufbauen</name>
  <read_first>
    src/stores/captureStore.ts
    src/db/database.ts
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-01-PLAN.md
  </read_first>
  <files>src/services/analysis/analysisService.ts, src/stores/analysisStore.ts, tests/unit/analysisStore.test.ts</files>
  <behavior>
    - Store initialisiert Zeitraum auf letzte 30 Tage (D-09).
    - Presets 7/30/90 aktualisieren den Zeitraum eindeutig (D-10).
    - `resetFilters()` setzt immer auf 30 Tage zurueck (D-11).
  </behavior>
  <action>Implementiere `createAnalysisService(...)` und `createAnalysisStore(...)` analog zum Capture-Pattern. Store-Felder: `rangePreset`, `fromDay`, `toDay`, `loading`, `error`, `intervals`, `pvDays`, `combined`, `quality`. Verknuepfe mit Domain-Funktionen aus Plan 02-01. Standard-Preset beim Start: `30` Tage; `setPreset(7|30|90)` und `resetFilters()` muessen vorhanden sein und D-09..D-11 strikt umsetzen.</action>
  <verify>
    <automated>node --test tests/unit/analysisStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `analysisStore.test.ts` prueft explizit Presets 7, 30, 90.
    - `analysisStore.ts` enthaelt `resetFilters()` und setzt `rangePreset = 30`.
    - Bei Fehlern setzt der Store `error` auf nicht-leeren String und `loading` auf `false`.
  </acceptance_criteria>
  <done>Analysezustand ist reproduzierbar und folgt den gelockten Zeitraumentscheidungen.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Zeitraum-UI und getrennte Ergebnislisten implementieren</name>
  <read_first>
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
    src/features/meter/MeterReadingsList.vue
    src/features/pv/PvDailyList.vue
    src/stores/analysisStore.ts
  </read_first>
  <files>src/features/analysis/AnalysisRangeCard.vue, src/features/analysis/IntervalList.vue, src/features/analysis/PvDaySummaryList.vue, tests/component/analysisRangeCard.test.ts, tests/component/intervalList.test.ts</files>
  <action>Baue drei mobile-first Komponenten im manuellen CSS-Stil (keine neue UI-Bibliothek, kein Tailwind): `AnalysisRangeCard` mit Preset-Buttons `7 Tage`, `30 Tage`, `90 Tage`, Datumsfeldern und Aktion `Filter zuruecksetzen`; `IntervalList` mit Feldern Start/Ende/Dauer/Bezug/Einspeisung/kWh pro Tag/Kosten; `PvDaySummaryList` als separater Block mit Label `Tageswert`. Fuer D-14 muss der Standardpreis (0.305 EUR/kWh) ausschliesslich als unaufdringlicher Detailhinweis im Kosten-Kontext der Intervallliste erscheinen (z. B. unterhalb/innerhalb der Kosten-Sektion) und darf NICHT als Badge, Titel, Hero oder Primarkennzahl gerendert werden. Kosten ohne Basis muessen exakt `Kosten noch nicht verfuegbar` zeigen (D-16). Intervallsortierung standardmaessig neueste zuerst (D-12).</action>
  <verify>
    <automated>node --test tests/component/analysisRangeCard.test.ts tests/component/intervalList.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `AnalysisRangeCard.vue` enthaelt die Texte `7 Tage`, `30 Tage`, `90 Tage`, `Filter zuruecksetzen`.
    - `PvDaySummaryList.vue` enthaelt das Wort `Tageswert`.
    - `IntervalList.vue` enthaelt den exakten Text `Kosten noch nicht verfuegbar`.
    - `IntervalList.vue` enthaelt einen Detailhinweis mit `0.305 EUR/kWh` bzw. `Standardpreis` im Kostenbereich (D-14), aber kein entsprechendes Badge/Heading im Primarbereich.
    - `tests/component/intervalList.test.ts` prueft explizit: Detailhinweis sichtbar, jedoch nicht als Haupttitel/Primarkennzahl gerendert (D-14).
    - Component-Test deckt Preset-Klick + Reset-Verhalten ab.
  </acceptance_criteria>
  <done>Analyseansicht ist bedienbar und trennt Intervall- von Tagesdaten sichtbar und fachlich korrekt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-Filter→Store | Benutzerwerte fuer Datum/Preset koennen ungueltig oder widerspruechlich sein |
| Store→Rendering | Leere/teilweise Daten koennen irrefuehrend dargestellt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | T | AnalysisRangeCard/store | mitigate | Validiere Datumsreihenfolge und numerische Presets, fallback auf 30 Tage statt undefiniertem Zustand |
| T-02-06 | I | IntervalList/PvDaySummaryList | mitigate | Erzwungene getrennte Darstellung (`Tageswert` separat) verhindert falsche Exaktheitsannahme |
| T-02-07 | D | resetFilters flow | mitigate | Reset bestaetigt klaren Zielzustand (30 Tage) und vermeidet unbeabsichtigte, irreparable Filterlage |
</threat_model>

<verification>
`node --test tests/unit/analysisStore.test.ts tests/component/analysisRangeCard.test.ts tests/component/intervalList.test.ts`
</verification>

<success_criteria>
Analyse kann mit Presets und Datumswahl gesteuert werden; Outputs sind getrennt, sortiert und mit ehrlichem Kostenfallback dargestellt.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyse-dashboard-datenqualitaet/02-02-SUMMARY.md`
</output>
