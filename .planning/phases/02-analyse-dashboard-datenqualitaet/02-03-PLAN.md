---
phase: 02-analyse-dashboard-datenqualitaet
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/router/index.ts
  - src/main.ts
  - src/App.vue
  - src/features/dashboard/DashboardView.vue
  - src/features/analysis/AnalysisView.vue
  - src/features/shell/AppShellNav.vue
  - tests/component/appShellNavRouter.test.ts
  - tests/component/dashboardView.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, ANLY-03, ANLY-04, QUAL-01, QUAL-02, QUAL-03]
must_haves:
  truths:
    - "App startet im Dashboard und bietet Bottom-Navigation Dashboard/Erfassung/Analyse"
    - "Dashboard zeigt leer/teilweise/befuellt mit klaren Schnellaktionen"
    - "Kombinierte KPIs sind als Naeherung sichtbar und bei poor abgegraut mit Warnung"
  artifacts:
    - path: "src/router/index.ts"
      provides: "App-Shell-Routing auf drei Ziele"
    - path: "src/features/dashboard/DashboardView.vue"
      provides: "Hero-KPI, Datenqualitaetskarte, Recency-Karten, EmptyState"
    - path: "src/features/shell/AppShellNav.vue"
      provides: "Bottom-Navigation mit Textlabels"
  key_links:
    - from: "router/index.ts"
      to: "DashboardView/AnalysisView/CaptureView"
      via: "route records"
      pattern: "/dashboard|/capture|/analysis"
    - from: "AnalysisView"
      to: "analysisStore"
      via: "combined + quality rendering"
      pattern: "Naeherung|Schaetzung|good|limited|poor"
---

<objective>
UI-Shell, Dashboard und Analyseansicht gemaess verbindlicher UI-SPEC integrieren.

Purpose: Nutzer erhalten die sichtbaren Produktwerte der Phase 2 in einer mobilen, ehrlichen und statusklaren Oberflaeche.
Output: Router-basierte App-Shell mit Dashboard-/Analyse-Rendering, Quality- und Warning-UI.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
@.planning/phases/02-analyse-dashboard-datenqualitaet/02-CONTEXT.md
@src/App.vue
@src/main.ts
@src/features/capture/CaptureView.vue
@.planning/phases/01-datenerfassung-fachfundament/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Router und App-Shell-Navigation mit Dashboard-Default implementieren</name>
  <read_first>
    src/App.vue
    src/main.ts
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
    src/features/capture/CaptureView.vue
  </read_first>
  <files>src/router/index.ts, src/main.ts, src/App.vue, src/features/shell/AppShellNav.vue, tests/component/appShellNavRouter.test.ts</files>
  <behavior>
    - RED: `tests/component/appShellNavRouter.test.ts` beschreibt Router/App-Shell-Verhalten und failt zuerst: Redirect `/`→`/dashboard`, sichtbare Labels `Dashboard|Erfassung|Analyse`, Navigation auf `/capture` und `/analysis`.
    - GREEN: Implementierung in Router/Main/AppShellNav macht alle RED-Tests gruen, ohne unnoetige Zusatzlogik.
    - REFACTOR: Benennungen/Struktur in Router und Nav vereinfachen (z. B. klare Route-Namen/konstante Nav-Items), Tests bleiben durchgehend gruen.
  </behavior>
  <action>Fuehre Vue Router ein (ohne neue UI-Bibliothek) und setze Routen `/dashboard`, `/capture`, `/analysis` plus Redirect von `/` auf `/dashboard`. Binde `AppShellNav` als mobile Bottom-Navigation mit reinen Textlabels `Dashboard`, `Erfassung`, `Analyse` ein. `Erfassung` muss bestehende Phase-1-Capture-Ansicht unveraendert nutzen. Verpflichte die Umsetzung auf TDD-Workflow gemaess AGENTS.md/tdd-workflow: zuerst RED-Testdatei `tests/component/appShellNavRouter.test.ts`, dann GREEN-Implementierung, danach REFACTOR bei gruenem Teststand.</action>
  <verify>
    <automated>node --test tests/component/appShellNavRouter.test.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tests/component/appShellNavRouter.test.ts` existiert und enthaelt mindestens je einen Test fuer Redirect `/`→`/dashboard` und Nav-Ziele `/capture` + `/analysis`.
    - RED ist nachweisbar: initialer Testlauf failt vor Router/Nav-Implementierung; danach GREEN mit erfolgreichem Testlauf.
    - REFACTOR ist nachweisbar: nach Struktur-/Naming-Bereinigung bleiben alle Task-1-Tests gruen.
    - `src/router/index.ts` enthaelt drei Route-Pfade `/dashboard`, `/capture`, `/analysis`.
    - `AppShellNav.vue` enthaelt exakt die Labels `Dashboard`, `Erfassung`, `Analyse`.
    - `src/main.ts` verwendet `app.use(router)` vor `mount`.
  </acceptance_criteria>
  <done>App-Shell ist lauffaehig und navigierbar mit Dashboard als Startpunkt.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Dashboard- und Analyse-Views mit Zustandskarten, Naeherungslabel und Warnungen umsetzen</name>
  <read_first>
    .planning/phases/02-analyse-dashboard-datenqualitaet/02-UI-SPEC.md
    src/stores/analysisStore.ts
    src/features/analysis/AnalysisRangeCard.vue
    src/features/analysis/IntervalList.vue
    src/features/analysis/PvDaySummaryList.vue
  </read_first>
  <files>src/features/dashboard/DashboardView.vue, src/features/analysis/AnalysisView.vue, tests/component/dashboardView.test.ts</files>
  <action>Implementiere UI-SPEC-konforme Zustandsdarstellung: Dashboard-Empty-State mit `Noch keine Auswertung moeglich` und Aktionen `Zaehlerstand erfassen` + `PV-Tageswert erfassen` (DASH-01), befuelltes Dashboard mit letzter Ablesung, letztem PV-Tageswert, KPI-Hero, Qualitaetskarte (DASH-02). In AnalysisView: sichtbares Label `Schaetzung`/`Naeherung` bei kombinierten KPIs (ANLY-03/04), `good|limited|poor` inkl. konkreter Gruende (QUAL-01/02), persistente Warnkarte bei `PV < Einspeisung` oberhalb kombinierter KPIs (QUAL-03; D-08), sowie abgegraute KPI-Karten bei `poor` (D-04). D-14 absichern: In Dashboard/Analysis darf der Standardpreis nie als Hero, Hauptbadge oder Headline auftauchen; Preisbasis bleibt nur Detailhinweis im Kostenkontext (bereitgestellt in 02-02).</action>
  <verify>
    <automated>node --test tests/component/dashboardView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `DashboardView.vue` enthaelt `Noch keine Auswertung moeglich` und beide Quick-Action-Texte.
    - `AnalysisView.vue` enthaelt mindestens eines der Woerter `Schaetzung` oder `Naeherung`.
    - `AnalysisView.vue` enthaelt den Satzbeginn `Plausibilitaetswarnung: Einspeisung liegt ueber dem erfassten PV-Tagesertrag.`
    - `AnalysisView.vue` enthaelt die drei Labels `good`, `limited`, `poor`.
    - `DashboardView.vue` und `AnalysisView.vue` enthalten keine primare Hervorhebung `Standardpreis`/`0.305 EUR/kWh` als Titel, Hero oder Badge (D-14).
  </acceptance_criteria>
  <done>Dashboard und Analyse liefern alle geforderten UI-Zustaende mit transparenter Unsicherheitskommunikation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router/Nav→Screen state | Falsche Navigation kann Nutzer in inkonsistente Analysezustaende fuehren |
| Quality/KPI text→User decision | Missverstaendliche Texte koennen Fehlentscheidungen beguenstigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | D | AppShellNav/router | mitigate | Feste drei Hauptziele mit klarem Default `/dashboard`; keine versteckten destruktiven Aktionen in Navigation |
| T-02-09 | I | AnalysisView KPI cards | mitigate | Zwingende Labels `Schaetzung/Naeherung` und Quality-Card mit Gruenden verhindern Scheingenauigkeit |
| T-02-10 | T | Dashboard/Analysis state mapping | mitigate | Explizite Empty/Partial/Error-Zustaende statt impliziter Nullwerte oder stiller Defaults |
</threat_model>

<verification>
`node --test tests/component/appShellNavRouter.test.ts tests/component/dashboardView.test.ts && npm run build`
</verification>

<success_criteria>
Nutzer koennen ueber App-Shell navigieren und erhalten in Dashboard/Analyse nachvollziehbare Kennzahlen mit verpflichtender Unsicherheits- und Qualitaetskommunikation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-analyse-dashboard-datenqualitaet/02-03-SUMMARY.md`
</output>
